# Decision Log — Solo Developer

**Purpose:** Record strategic decisions so future sessions don't re-litigate settled questions. Append-only. Each entry has a date, a decision, the reasoning, and what it excludes.

**Format:**
```
YYYY-MM-DD | Decision | Rationale | Excludes
```

---

## 2026-07-26

| # | Decision | Rationale | Excludes |
|---|---|---|---|
| 001 | **Portfolio first, then vertical slice.** Phase 1 = ship the portfolio website. Phase 2 = ship a 3-5 minute playable encounter. | The portfolio pipeline is 90% complete structurally but has never produced a valid output. Fixing it provides visual proof of capability and unblocks everything. The vertical slice proves the game loop exists. | No more platform-building without a delivery target. No simultaneous phases. |
| 002 | **Solo developer — strip team-simulation overhead.** No agent ownership boundaries, no interface contracts, no sentinel runners, no loop health monitors, no STOP files. Direct access to everything. | The 5-agent architecture was designed to prevent 5 people from conflicting. One person doesn't need it. The ceremony consumes 15-30% of session capacity. | All agent coordination docs (AGENT_BOUNDARIES.md, AGENT_OPERATING_MODEL.md, AGENT_OWNERSHIP.md, AGENTS.md) are historical — read for context, not for process. |
| 003 | **Ship the portfolio to a live website, not a GitHub repo.** Fix the pipeline, capture real renders, publish. | The portfolio pipeline's purpose is to produce a website. A GitHub repo with unprocessed JSON files is not a portfolio. | Don't polish the pipeline beyond what's needed to produce a working site. |
| 004 | **Vertical slice ships to itch.io.** Not Steam, not Gumroad, not GitHub Releases. | itch.io is zero-barrier to publish, discoverable, and accepted as a "demo" format. Perfect for a solo 5-minute experience. | Don't set up Steam store page. Don't build Gumroad product. |
| 005 | **`L_WP_SakuraDream` is the vertical slice level.** Most visually complete, cozy tone, existing assets ready. | The 4 WP levels were profiled: SakuraDream has the most production-ready content. Using it minimizes new work. | No new level creation. No adapting a different pillar. |
| 006 | **MelodiaCore C++ plugin gets a 5-day compile budget. If it fails, pivot to Python runtime.** | The C++ plugin is the critical path for gameplay. But 808 lines of GMM Python battle code + Blueprint calls to Python is a viable fallback that can ship faster. | Don't spend more than 5 days fighting compilation errors. Ship the fallback. |
| 007 | **Known limitations are documented, not fixed.** Stats exporter missing, some render captures placeholder, PCG crash graphs quarantined, material system review not actioned. | The intake report identified 9 P0 actions across both phases. Everything else is parked. The project will ship with known gaps clearly documented. | Don't delay shipping to fix non-blocking issues. |

## 2026-07-29

| # | Decision | Rationale | Excludes |
|---|---|---|---|
| 008 | **Vertical slice ships first; portfolio via AI agent delegation.** The playable First Dream route (Morning → Dreamstate → ZenForest) is PIE-verified and is the project's strongest asset. Portfolio renders are being produced in parallel by delegated AI agents; user supplies renders for website push. | The 2026-07-28 PIE-verified opening traversal proves the game loop works. Portfolio is not abandoned — it's parallel work via delegation. User will supply renders for website push when ready. | Portfolio is not blocked — it's parallel work. No need to choose one over the other. Both tracks active simultaneously. |
| 009 | **Co-op skills use stock JRPG battle authority.** Petal Cadence and Skybound Refrain are true `BP_BattleSkillBase` children. Resonance is a `BP_BuffBase` child. No MelodiaCore combat controller owns outcomes. | Stock JRPG template owns turn scheduling, damage, buffs, and results. Adding a second combat authority would duplicate work and risk desync. | No custom damage callbacks, no rhythm judgement, no MelodiaCore battle override. Stock authority is the single source of truth. |
| 010 | **Hair attachment fix uses native C++ fallback, not re-export.** Body (465 bones) and hair (148 bones) share zero bone names. Copy Pose From Mesh cannot work. Fix: attach hair component to `head_x` via `UMelodiaHairComponent`, retain Kawaii Physics below `hair_root`. | Re-exporting the hair against the body armature is the correct long-term fix but requires Blender work. The native fallback unblocks combat visibility now. | Don't block gameplay on Blender re-export. Don't rename `hair_root` — it's the approved Kawaii root. |

## 2026-07-30

| # | Decision | Rationale | Excludes |
|---|---|---|---|
| 011 | **RhythmCombatComponent/RhythmInputComponent/RhythmCombatUI quarantined, not integrated.** Moved to `Source/BS_GodFile/_Quarantine/RhythmCombat_20260730/`. Rhythm/skill integration going forward uses Harmonix per `Docs/HARMONIX_MIDI_RHYTHM_CONTRACT_2026-07-29.md`, updating individual skills to consume it, not a parallel combat-authority component. | These components computed their own damage multipliers and combo state directly from input accuracy — a direct violation of Decision 009 (no custom damage callbacks, stock JRPG authority only), built the same day as 009 with no cross-lane check. Confirmed with the project owner before quarantining. | `RhythmBeatTracker` (BPM/beat/bar tracking only, no damage logic) is unaffected and stays available for Harmonix-based presentation work. Do not restore the quarantined trio without an explicit new decision reopening custom rhythm-combat authority. |
| 012 | **All musical time is delegated to Harmonix and Quartz; wall-clock beat accumulators are banned.** `UMelodiaMusicClockSubsystem` is the project's single source of musical time: Harmonix (authored tempo map) preferred, Quartz (sample-accurate battle transport) second, nothing third. When neither runs, `HasMusicalTime()` is false and presentation degrades rather than inventing a beat. | The project had four competing notions of "the beat" and the one actually running was a hardcoded 120 BPM wall-clock accumulator, against 128 BPM source music — every beat drawn was wrong, and `DeltaTime` accumulation drifts audibly within a minute. Harmonix also supplies the calibrated timebases (`ExperiencedTime` for input, `VideoRenderTime` for visuals) that decide whether the layer feels tight. | No component may own a BPM or accumulate `DeltaTime` into a beat. `RhythmBeatTracker` keeps its Blueprint pins but is now a forwarder. The subsystem is presentation-only — it must never be wired into damage, turns, or results (Decisions 009/011 stand). |

| 013 | **`FMelodiaNarrativeRecord` is version 2 and is the sole store for Persona-lite persistent state.** Added `SocialStats` (canonical — `UMelodiaPersonaSubsystem` no longer keeps a transient copy), plus reserved `BondRanks`, `PhaseIndex`, `SpawnContext`. `MigrateRecord` steps old records forward one version at a time and refuses records newer than the build. | Social stats were `Transient` with no record field, so the Persona-lite loop could not close — stats evaporated on reload. Schema fields are free to add while `Version == 1` and nothing has shipped; after saves exist they cost an upgrade path forever. `RestoreNarrativeRecord` previously wiped on any version mismatch, so a bump without migration would have destroyed existing saves. | No second persistence store. Player preferences (volumes, calibration offset, reduced motion, UI scale) stay in `UMelodiaGameUserSettings` — they are not campaign state. Never bump `CurrentVersion` without adding the matching `MigrateRecord` case. |
| 014 | **One quest-gating predicate: `UMelodiaPersonaSubsystem::IsGatedContentAvailable`.** Minimap markers and the planned Orrery travel adapter both call it rather than each implementing the rule. | Marker visibility and destination unlock ask the identical question ("is content requiring this quest available?"). Two implementations would drift. | Do not add a second gating rule for a third consumer; extend this one. |
| 015 | **Musical time is available project-wide, not only in battle.** `UMelodiaMusicClockSubsystem::Get` / `GetMusicBeatPhase` / `GetMusicPulse` are static one-node accessors, and the audio-reactive MPC publishes `BeatPhase` whenever any clock runs. | Ambient life (lantern flicker, idle sway, petal drift, UI breathing) synced to music makes the world feel scored at near-zero cost, and matches the Persona-lite tone. Battle intensity remains gated on battle. | Still presentation-only. Wider availability does not relax Decisions 009/011 — no consumer may route this into damage, turns, or results. |

| 016 | **Rhythm is expressive, not evaluative. There is no miss penalty, ever.** Good timing adds flourish and a small stock-legal bonus; bad timing yields the normal stock result. The rhythm layer never gates, blocks, or reduces an outcome. | OMORI shows RPG depth comes from planning, not execution pressure; Infinity Nikki's cozy audience overlaps Melodia's and treats failure as cost rather than challenge; `_VERTICAL_SLICE_SCOPE.md` already required rhythm never be mandatory. This also settles the ceiling on `PresentationScalar`, which unblocks profile authoring. | `melodia_rules.json` still encodes the old evaluative model (`grade_multipliers` bottoming at 0.55 — a 45% miss penalty). That data is now historical for the JRPG lane and must not be ported into stock content. An evaluative mode, if ever wanted, is a difficulty option — never the default. The battle UI must never display "MISS". |
| 017 | **Resonance is *the* combat decision.** Petal Cadence applies it; Skybound Refrain exploits it. One readable state, applied by one action, exploited by another. | It is the OMORI structure (one status axis carrying the whole game) expressed in Melodia's own theme — music answering music — and it is already stock-native and one Blueprint branch from done. | Toughness/break and elements stay deferred despite being fully specified in `melodia_rules.json`; per the stop rule, Resonance's decision loop must be proven fun before a second mechanic is added. Corollary: Resonance must be *visible* in the battle UI — an invisible one-turn buff is a mechanic the player never learns. |
| 018 | **Social stats are raised by Quill dialogue choices only**, via allowlisted `melodia:` intents. Not combat, not pickups, not exploration. | One source keeps the currency legible and keeps the intent allowlist as the single validation choke point. Stats gate quest availability, which gates markers, which lead to encounters — the loop closes with one wire. | No stat gain from battle results, item pickup, or traversal. Adding a second source requires a new decision. |
| 019 | **Autosave fires at exactly three boundaries:** after a completed battle result (post-terminal, pre-control-return), on authored map travel, and on quest state change. Writes go to a temp file and are atomically renamed. | Predictable save points are a QOL requirement already in `_VERTICAL_SLICE_SCOPE.md`; atomic rename prevents a crash mid-write from destroying the only slot. | Never mid-battle (already gated), never mid-dialogue. No autosave on a timer. |
| 020 | **Quarantined-lane MelodiaCore classes are guarded in place, not moved.** 13 classes (`MelodiaSaveGame*`, `MelodiaQuestManagerBase`, `MelodiaGameMode*`, roguelike stack, `MelodiaEntitlementSubsystem`, `MelodiaOutfitComponent`, second battle stack) now carry `NotBlueprintable, NotPlaceable, HideDropdown` and lost `BlueprintSpawnableComponent`. | The originally planned bulk move to `_Reference/` was **unsafe**: a content scan proved `.umap`/`.uasset` packages reference classes that a C++-only grep reported as dead. Guarding blocks *new* second authorities — the actual goal — while every existing reference still resolves. | Existing content references are untouched and still work. Subsystems still auto-instantiate; gating those needs `ShouldCreateSubsystem` and a separate decision. A bulk move remains possible only after a complete content-reference audit. |
| 021 | **Three existing content references to quarantined-lane classes are logged for editor cleanup**, not silently removed: `ZenForestTest.umap` → `MelodiaDungeonRunCoordinator`; `L_MelusinaMorning.umap` → `MelodiaGameMode`; ~~`BP_Melusina.uasset` → `MelodiaOutfitComponent`~~ **RESOLVED 2026-07-31** — see 029g/034. | These are real second-authority leaks already inside the shipping route, found by binary content scan. Removing them is editor work with real regression risk, so it is scheduled rather than done blind. | Do not delete these references from a script. Open each asset, confirm what depends on it, and remove or replace deliberately. Two of the three remain open. |
| 035 | **Build green, 2026-07-31 evening: exploration-gate + Stage C (authority locator) + Stage D (pacing) + Decision 034 quarantine, one batch, 46/3 regression baseline unchanged.** Two real fixes landed en route, both narrow: (1) `MelodiaSharedAuthorityInterfaces.h`'s four interface `UFUNCTION`s were declared `BlueprintPure`, which UHT rejects outright on an interface function — changed to `BlueprintCallable` on the interface declarations only; the concrete overrides in `UMelodiaTravelSubsystem`/`UMelodiaInputContextSubsystem` keep their original `BlueprintPure`, unaffected. (2) DeepSeek's staged `MelodiaOllamaValidation.cpp` (the async HTTP replacement for the earlier unsafe `_popen` probe) landed on disk mid-session and failed to compile — its callback lambda used `Message` without capturing it. Fixed by adding `Message` to the capture list, by value (the callback outlives the function that queued it, so a reference would dangle). Nothing else about that file was touched. | The interface-`BlueprintPure` rule is a genuine UHT constraint a static code read can miss — a pre-build review pass this session (Sonnet, read-only) reported no compile-breakers and was wrong on exactly this point; the compiler is authoritative, the review was a useful second pass but not a substitute. The Ollama fix was made to unblock a shared build others depend on, not as commentary on that file's design — the design itself (async HTTP replacing a blocking, shell-injectable `_popen` call) is correct and was already the right direction. | Do not read this as approval to freely edit other agents' staged files — this was a one-line mechanical fix to a build-blocking compile error, made because leaving the build red blocks everyone's next build, not a precedent for touching in-flight work outside a shared blocker. |
| 034 | **`BP_Melusina.uasset` + its 2026-07-29 backup quarantined** (`_QuarantineAssets_20260731/`), not deleted — closes the Decision 021 `MelodiaOutfitComponent` leak. `Config/DefaultEngine.ini`'s `PackageRedirect` for the old `/Game/Characters/Melusina/BP_Melusina` path removed in the same change (kept `ABP_Melusina`/`IK_Melusina`/`SK_Melusina` redirects — untouched). | Reference check (Monolith, live editor, this session) found zero `.umap` references and two `.uasset` referencer chains (`BP_MelodiaGameMode`, `WBP_Battle_Rhythm`), both dead-ending at a root-level `/Game/L_MelusinaMorning` map that itself has zero referencers — not the live route map, not loaded by anything. Stop condition from Decision 029g's plan ("if a live-route map references it, stop") did not trigger. `BP_MelusinaJRPGCharacter` (canonical pawn) already gained its own `MelodiaOutfitComponent` this session, so outfit capability is not lost. | The redirect was removed, not repointed — it existed to survive a prior move, not to keep resolving after retirement; repointing it would let a stale reference silently resolve to a package no longer in `Content/`. A third, separate `BP_Melusina` variant at `/Game/Characters/Melusina/BP_Melusina` (2026-07-16, zero referencers) was found but **not** touched by this quarantine — flagged in `_ROADBLOCKS_2026-07-31.md` for a future pass, not resolved here. |
| 037 | **DeepSeek's 6-site `OpenLevel`→`TravelTo` reroute (`OrreryMainMenuGameMode.cpp`, `MelodiaOpeningPortal.cpp`) build-confirmed, 2026-07-31 late evening.** Closed-editor `Build.bat` against current source: target already up to date, zero errors. `Automation RunTests Melodia` (full suite): **46 pass / 3 fail**, same three known pre-existing failures (`Melodia.NPC.InteractionDefaults`, both `Melodia.Roguelike.Functional.*`), zero new failures. | This work had landed on disk (confirmed by file mtimes matching the handoff's specified pattern) but was unverified — no rebuild or regression rerun had been logged since. Per this project's own repeated lesson this session, code on disk is a claim, not proof; this closes that gap for item 2 of the DeepSeek evening handoff. | Item 1 (Ollama probe wiring) and item 3 (remaining pacing-float migration) from the same handoff are still unconfirmed — this decision covers only item 2. |
| 038 | **`PPV_NikkiDream` was inert on every level that had it placed — the render-polish pass found and fixed the real cause.** `setup_nikki_render_post_process.py`'s `LEVELS` allowlist never covered any of the 4 website-render target levels (`L_KaleidoNave`, `L_FallenMoon`, `L_MelusinaMorning`, `ZenForestTest`); whoever hand-placed `PPV_NikkiDream` on `L_KaleidoNave`/`L_FallenMoon` set chromatic aberration to a stray `1.2` with the override flag left **off** (so it wasn't even applying) and the volume's 3 signature blendable materials (`M_PP_ToonOutline`, `M_PP_StorybookVines_Inst`, `M_PP_MeluColorGrade`) were never attached at all — `weighted_blendables` was empty on every instance checked. Fixed by extending the script's `LEVELS` tuple to include all 4 target levels and re-running it: this is idempotent and authoritative, so it corrected CA to the canonical `0.35`, attached all 3 blendables (all resolved, zero missing), set priority to `10.0` uniformly, and added the volume fresh to `L_MelusinaMorning`/`ZenForestTest`, which had none. | The owner flagged "chromatic aberration is jacked up" while retuning by hand; tracing it found the real bug was structural (missing blendables + a dead override flag), not a value that needed nudging — nudging the `1.2` down would have "fixed" the symptom while leaving the volume permanently blind to its own intended grade materials. `L_KaleidoNave`'s second volume (`NavePost`, priority 0, unbound) was also checked — no real conflict, `PPV_NikkiDream`'s priority `10.0` already wins the blend; `NavePost` is vestigial, left untouched. | `L_MelusinaMorning` also got a new `CameraActor` (`CAM_MelusinaMorning_Portfolio`) since it had zero placed cameras — framing is a first pass (established by room-layout coordinates, not visually confirmed) and may need adjustment during actual capture. `ZenForestTest`'s existing `CineCameraActor` was checked against all 3 `MelodiaNPCPlaceholder` cylinder locations and found already ~8,900+ units from the nearest — left as-is, not repositioned. |
| 039 | **Set-dressing/VFX pass on the 4 website-render levels, 2026-07-31/08-01 night.** `L_KaleidoNave`: added 3 symmetric Ornament-kit arch pairs (`SM_Orn_QuatrefoilArch`/`GothicTracery`/`RosetteMedallion`) at the 3 previously-bare light positions (x=4000/12000/20000) plus 2 more `NS_SakuraWaterPetals` instances (x=8000/24000) to spread `FX_NavePetals`' coverage across the ~30,800-unit corridor instead of leaving it concentrated at the midpoint. `L_MelusinaMorning`: added `NS_Uni_DustShafts` through the threshold pillars and `NS_Uni_PollenSparkle` over the bed. `ZenForestTest`: added `NS_SakuraPetals_v2` near the torii cluster and `NS_Uni_Fireflies` near the tea house — this level had zero VFX actors before. `L_FallenMoon` untouched — already had 3 VFX systems and full dressing from before this session. | Owner asked directly to set-dress and polish the 4 render-target levels tonight; this is additive-only (no existing content removed or modified), reusing entirely existing kit assets already in the project (Ornament meshes, `EnvSandbox/VFX/Systems/*` library) per the render-polish plan's own "reuse, don't invent" scope. | **`L_MelusinaMorning` is listed in `CLAUDE.md`'s Red tier ("Change `L_MelusinaMorning` content — Ask First").** The owner's own message this session ("can you help me set dress and polish the levels") is the explicit human instruction the never-touch table's own exception clause requires, given this level was already one of the 4 named render targets under active discussion — flagging this plainly rather than silently treating the Red tag as satisfied by implication. All 4 levels saved; none of this has been build- or PIE-verified since it's pure level-content (no C++/Blueprint graph changes), consistent with the render-polish plan's own verification section. |
| 040 | **Root-caused and fixed why the render-polish levels looked darker after Decision 038/039, 2026-08-01.** `setup_nikki_render_post_process.py`'s `_configure()` was idempotent but not additive — every run unconditionally overwrote `PPV_NikkiDream`'s full grade on every level in its `LEVELS` tuple (bloom 0.65 vs engine default ~1.0, vignette 0.22 vs default 0.0, shadow-gain below 1.0, contrast above 1.0, CA 0.35 vs default 0.0), including `L_FallenMoon` which already had a configured/possibly hand-tuned volume before Decision 038's run — that prior tuning was silently lost, and the audit JSON never recorded values, only which blendables loaded. **Fixed the script** (`Content/Python/setup_nikki_render_post_process.py`): added a guard — if a level's `PPV_NikkiDream` already has `override_bloom_intensity=True` (already tuned), a normal run now only attaches missing blendables and leaves existing values alone; `force=True` still reasserts the canonical preset when explicitly wanted. **Then retuned all 4 levels with distinct, level-appropriate grades** instead of the one global preset: `L_MelusinaMorning` (literal morning scene) bright/high-key — bloom 1.1, vignette 0.05, no CA/grain; `L_KaleidoNave` (dreamlike corridor) luminous — bloom 0.9, vignette 0.15; `ZenForestTest` (forest/dungeon) moderate mood — bloom 0.8, vignette 0.2; `L_FallenMoon` (night/celestial) moodier but with bloom un-cut (1.0, not reduced) since the aurora/constellation VFX wants bloom to read. | Owner asked directly "did you make the levels darker" after the pass — yes, mechanically, confirmed by reading the script rather than guessing. A script written to fix one bug (missing blendables) silently reasserting a full artistic grade on every run is a real defect independent of today's dressing work; fixing the script itself, not just the symptom on 4 levels, prevents this recurring the next time anyone re-runs it for any reason. | Exact prior `L_FallenMoon` values before Decision 038's run are unrecoverable — the audit log never captured them. The new `L_FallenMoon` grade is a deliberate re-decision (moodier vignette/contrast kept as fitting a night/celestial setting, bloom explicitly restored), not a reconstruction of what was there before. All 4 grades are first-pass, chosen from reasoning/reference (`Docs/PCG_WP_INFINITY_NIKKI_GREYBOX_PLAN.md`, the `niagara_nikki_library.json` warm-palette manifest) rather than visually confirmed in-viewport — the screenshot-capture pipeline from this session remains unreliable (`HighResShot` and `AutomationLibrary.take_high_res_screenshot` both failed to flush to disk); eyeballing in the live editor is still owed before calling any grade final. |
| 041 | **Root-caused the remaining "still looks dark" report to Ultra Dynamic Sky, not the PPV — fixed on all 4 render-target levels, 2026-08-01.** Every level's `Ultra_Dynamic_Sky` actor had `Instant Exposure Adjustment on Begin Play = False` — auto-exposure (histogram metering, -10 to +20 EV range, matching what showed up on the PPV as inherited/non-overridden) eases in gradually on level load instead of snapping to correct brightness, so viewing a level shortly after opening it reads darker than steady-state. All per-weather `Exposure Bias` values (Day/Night/Cloudy/Interior/etc.) were confirmed neutral at `0.0` — not misconfigured, ruling out a UDS exposure-bias mistake. Flipped `Instant Exposure Adjustment on Begin Play` to `True` on all 4 levels' UDS actors, saved. | Decision 040 fixed the PPV grade, but the owner reported the levels still looked dark afterward. `PPV_NikkiDream` has zero exposure overrides on any level (confirmed via reflection) — none of Decision 040's per-level grade values touch exposure at all, so the residual darkness had a separate cause. Histogram auto-exposure is also known to underexpose interior-heavy shots when any bright sky is in frame, compounding the slow-convergence issue for `L_MelusinaMorning` specifically. | This does not change how the scene looks once exposure has converged (a few seconds after load) — it only removes the "looks dark right when you open it" symptom. If a level still reads dark after a few seconds of settle time, that's a separate, real grade/lighting issue, not this one. |
| 042 | **Retired `PPV_NikkiDream`'s color-grading role in favor of the project's real, already-built Nikki grade system — root cause of the "levels look dark" report closed, 2026-08-01.** Owner confirmed empirically (toggling the volume invisible restored correct lighting) that the PPV itself, not exposure, was the cause. Research found `M_Master_Toon_Universal` already has a full "Nikki" material parameter group (`DreamSaturation`/`DreamContrast`/`PastelLift`/`RimIntensity`/etc, `MATERIAL_PIPELINE.md:32`, `setup_master_universal.py:1109-1196`) defaulting to neutral/off by design, plus an unused scene-cohesion `MPC_Portfolio_Palette` (`setup_portfolio_mpc.py`) the master already reads from — `PPV_NikkiDream`'s hardcoded `color_saturation`/`color_contrast`/`color_gain_shadows`/`color_gain_highlights` literals were a second, uncoordinated grading layer duplicating and fighting this real system. **Fixed:** removed those 4 overrides from `setup_nikki_render_post_process.py` entirely (bloom/vignette/grain/CA now default near-neutral: 1.0/0.0/0.0/0.0), reran with `force=True` on all 4 target levels, and explicitly cleared the leftover `override_color_*` flags from this session's earlier manual per-level retuning pass (Decision 040) that the script's normal run wouldn't have touched. **Second, deeper finding along the way:** one of the volume's 3 blendable materials, `M_PP_MeluColorGrade`, had its own always-on radial vignette (30% max corner darkening) **hardcoded directly in a Custom HLSL node** with zero exposed parameters — invisible to any PPV-level fix. Owner chose to expose it rather than hide/zero it: added `VignetteBaseIntensity` (default 0.3) and `VignetteFalloff` (default 3.5) as real scalar parameters wired into the existing formula, same default behavior, now tunable/off-able per instance. `GlobalEmissiveBoost` and `RhythmPulse` (also read by this material, from `MPC_Melodia_Palette`) were checked and confirmed neutral (1.0, 0.0) — not contributors. | The owner's own test isolated the PPV as the cause; simply zeroing scalar overrides wasn't enough because (a) this session's own earlier manual retuning left stale override flags the script wouldn't clear on a normal run, and (b) one blendable material had a real effect baked into its shader code, invisible to any FPostProcessSettings-level fix. Fixing both, plus tying future grading to the material system's own designed mechanism (MPC/per-instance Nikki params) rather than a third hardcoded preset, closes this properly rather than papering over one visible symptom. | `MPC_Portfolio_Palette` wiring into the 4 render-target levels (this plan's steps 2-3 — scene-wide MPC tuning per level, per-hero-asset Nikki material instance tuning) is not yet done — this decision covers only the PPV/blendable-material root-cause fix. Compilation and parameter exposure confirmed via `material_query get_compilation_stats`/`get_material_parameters`; visual confirmation still needs the owner's own eyes, per this session's standing screenshot-pipeline limitation. |
| 021b | ~~**CONFIRMED 2026-07-31 — the owning Blueprint is `BP_DefeatDialogue`**~~ **RESOLVED 2026-07-31 late evening.** `BP_DefeatDialogue`'s `OpenLevel(mainMenuMapName)` pin is fed by a connected `Get mainMenuMapName` from `BP_BattleController`, not the widget's own default — re-tracing found the widget's own default ("MainMenu") was a red herring; the value that actually reaches `OpenLevel` at runtime is `BP_BattleController`'s CDO, which was **`None`** (no `Set` node anywhere in the indexed project ever assigns it — confirmed via `get_cdo_properties` and a project-wide `Get`-only search). Fixed by setting `BP_BattleController`'s CDO `mainMenuMapName = /Game/Melodia/Levels/Menu/L_MelodiaMainMenu` — the owner confirmed this map is the real, functioning main menu (settings, save/load, start game), not a placeholder, despite its two visible test cubes. Compiled clean, saved. | Same shape as the three above: a stock-template default outliving the point where Melodia's own system should own it. The original diagnosis (widget default = literal "MainMenu") was plausible but wrong once the pin's actual data source was traced — a CDO of `None` is equally broken, just via a different mechanism (a no-op/failed travel rather than a wrong destination). | This is a CDO-level, single-property, zero-graph-risk fix — no Blueprint graph topology was touched, matching Decision 021's "deliberate, in-editor, one at a time" rule without the risk a graph edit would carry. Not yet PIE-verified end to end (party wipe → confirm → arrival at `L_MelodiaMainMenu`) — that live confirmation is still owed, see Stage 4 of the foundation-gate closure plan. |

| 022 | **Cook exit 25 resolved by quarantining five damaged assets, not by config exclusions.** Moved to `_QuarantineAssets_20260730/` (paths mirrored, reversible): `PCGEx_PathTesselate.uasset` (invalid name at index 411), `StaticClouds_A.uasset` (truncated at exactly 8 MiB), `Craters_1_-_512x512.uasset` and `Radial_8_-_512x512.uasset` (wrong/garbage headers), `SM_wallhi.uasset` (zeroed header). First successful package: `Saved/StagedBuilds_20260730/`, 2.1 GB, all five maps. | The `UnrealNames.cpp:2428` assert came from one plugin asset with a corrupt name-table entry, not from cook-time name composition. A `+DirectoriesToNeverCook=(Path="/PCGExtendedToolkit")` entry was tried first and **reverted**: blacklisting a plugin path collides with that plugin's asset-reference domain, raising a handled ensure that the cook tallies as ~13 errors — trading one fatal assert for a failing error count. A full header scan found only 3 corrupt files in 11,425 (0.026%), so the damage is contained, not systemic. | Do not re-add `DirectoriesToNeverCook` for a plugin path. Do not delete the quarantined assets — two are replaceable third-party texture packs, one is a truncated third-party texture, one is an unreferenced mesh, one is a plugin subgraph. None are referenced by the cook set. |

| 023 | **`UMelodiaTravelSubsystem` is the single travel authority.** Every teleport source — overlap volume, Quill intent, Orrery UI, dialogue choice, Blueprint travel node — calls `TravelTo(LevelId, SpawnTag)`, which records the arrival tag via `SetSpawnContext`, validates through `RequestTravel`'s `TravelLevelIds` allowlist, executes `OpenLevel`, force-clears the input-context stack on arrival, and places the pawn at the tagged `PlayerStart`. | Travel previously happened four ways, only one of which was validated, none of which placed the player deliberately, and the validated one had no consumer — `OnTravelRequested` broadcast into nothing. With a four-PlayerStart destination like KaleidoNave, arrival was a coin flip that would read as a bug long before it read as a spawn-point problem. | No component may call `OpenLevel` directly. `MelodiaMapTransitionComponent` was rewritten to route through `TravelTo` (it had been calling `LoadStreamLevel`, which silently does nothing for a standalone map). **Written down retroactively on 2026-07-31** — `MelodiaMapTransitionComponent.cpp:37` had cited "Decision 023" since 2026-07-30 while the log stopped at 022. ~~One known bypass remains: `MelodiaSaveSlotLibrary.cpp:50` hardcodes `OpenLevel` to `L_MelusinaMorning` as a no-`currentMap` fallback.~~ **ADDENDUM, 2026-07-31 — bypass closed.** `MelodiaSaveSlotLibrary.cpp:48-82` was the last direct `OpenLevel` in the project; its no-`currentMap` fallback now routes through `UMelodiaTravelSubsystem::TravelTo`, gaining allowlist validation, spawn placement, and the input-context clear. It degrades loudly, not silently: if the ID is refused (not in `TravelLevelIds`) or the subsystem is unavailable, it logs exactly what to add and only then falls back to `OpenLevel`, so a dead save slot never strands the player. Data-dependent note: whether `L_MelusinaMorning` (and the route maps) are actually **in** `DA_MelodiaIntegrationConfig -> TravelLevelIds` remains unverified from source (binary asset) — record it as unverified until a readback confirms it. |

## 2026-07-31

| # | Decision | Rationale | Excludes |
|---|---|---|---|
| 024 | **Agent-driven Blueprint wiring is verified, not trusted.** Monolith gained `get_graph_fingerprint` and `assert_graph_matches`; every graph edit follows export → fingerprint → mutate → compile → assert → fingerprint → save, one asset per transaction. A failed assertion returns success with `matched:false` so it is data the caller must branch on, never a swallowed error. | The five-item hand-wiring checklist existed because `BLUEPRINT_WIRING_CHECKLIST_2026-07-30.md` claimed graph topology could not be read reliably. It could — `export_graph` and `get_graph_data` read the live `UEdGraph`; the distrust came from misreading a staleness warning about the *asset index*. The real missing piece was not reading but **asserting**: `compare_blueprints` only compared node counts. `set_node_property`'s `ReconstructNode` drops links on renamed pins silently, so an assertion step is what makes automated wiring safe rather than merely possible. | Node identity in both actions is semantic (function+class, event name, variable name, macro, cast class), never `GetName()` — an assertion must survive delete-and-re-add. Fingerprints must be proven byte-stable across a no-op resave before anything relies on them; a flaky fingerprint teaches the agent to ignore assertions, which is worse than having none. Judgement stays human: the `melodia.Rhythm.Disable 1` A/B, "does the cursor release", and the two Decision 021 / stat-naming items are explicitly not automatable. |
| 025 | ~~**VibeUE stays disabled; Monolith is the only Unreal MCP surface.**~~ **REVERSED BY THE OWNER, 2026-07-31. Both Monolith and `it-is-unreal` (VibeUE) are registered in `.mcp.json` and enabled project-wide.** | The original rationale — VibeUE is unauditable, spends context, and duplicates `set_node_property` — was a *preference*, not a safety constraint, and it was applied unilaterally to a tool the owner actively uses. It also left the project with **no** working Unreal surface, since Monolith only loads on a fresh session. Availability beats architectural tidiness on the owner's own toolchain. | The one real caution survives: **do not run both surfaces against the same graph in one session.** Pick one per task. Monolith is preferred where both can do the job (namespace dispatch, full topology read, the `assert_graph_matches` verification loop); VibeUE covers things Monolith cannot, notably binding an `InputAction` to a node before `set_node_property` shipped. UnrealMCP (port 55557) remains unregistered. |
| 026 | **The Agent Working Agreement (`_AGENT_WORKING_AGREEMENT.md`) binds every agent and outranks every other agent doc in the repo.** Do the job asked and stop; never add a mechanism that compensates for a problem instead of removing its cause; "kill it" means delete and rebuild; do not re-verify what the owner has stated about their own assets; a fix request is not a review request. Replicated into `CLAUDE.md`, `AGENTS.md`, `.cursor/rules/`, `.kiro/steering/`, `.windsurf/rules.md`. | `UMelodiaHairComponent` cost roughly three days and ended as a **three-line** fix — snap the mesh to `head_x`. Everything removed along the way existed to compensate for something else: a runtime corrective transform cancelling a snap the same function performed, a `bForce…` bool overriding a guard, a branch concluding "touch nothing" followed by code that touched it, a one-tick defer papering over an ordering bug. No single step was incompetent; the accumulation was. The cost is measured in the owner's time and attention, not tokens — this is a portfolio and a livelihood, not a technical sandbox. | This is not a licence to under-deliver: the whole requested job still gets finished. It forbids *unrequested* expansion — investigation the owner did not ask for, designs where a repair was asked, reviews bundled into fixes, and compensation layers added instead of causes removed. If work is genuinely blocked, say so in a sentence or two and stop; do not substitute investigation for the blocked work. |
| 030 | **Cross-module authority access: an interface + locator layer in MelodiaCore, not physical relocation.** New `MelodiaSharedAuthorityInterfaces.h` (`IMelodiaTravelProvider`, `IMelodiaInputContextProvider`) + `UMelodiaAuthorityLocator` (`GameInstanceSubsystem`, MelodiaCore-native). `UMelodiaTravelSubsystem` and `UMelodiaInputContextSubsystem` (both `BS_GodFile`) now implement the interfaces and register with the locator in their own `Initialize()`. First consumer: `AMelodiaSirMelodiousIntroActor`'s departure now calls `TravelTo` through the locator, falling back to the pre-existing direct `OpenLevel` only if no provider is registered. | MelodiaCore's native C++ (`OrreryMainMenuGameMode`, Sir's intro actor, `MelodiaOpeningPortal`) could not call the game module's subsystems at all — `BS_GodFile.Build.cs` already depends on `MelodiaCore`, so the reverse dependency would be circular. Physically moving the subsystems into MelodiaCore was considered and rejected: it would change their `/Script/` class path and risk breaking every existing placed reference to systems that are live and were just fixed this session. The interface layer is additive — nothing that already works moves or changes identity. | The other 6 orphaned `OpenLevel` calls (`OrreryMainMenuGameMode` ×5, `MelodiaOpeningPortal`) are not rerouted yet — filed as a `_TASK_QUEUE.md` follow-up, not migrated blind in this pass. `Push`/`PopContext` are deliberately not on `IMelodiaInputContextProvider` — no MelodiaCore call site owns a context yet; add only when a real consumer needs it. Until Cline's wiring-checklist item 2a allowlists `L_KaleidoNave` in `TravelLevelIds`, `TravelTo` returns false and Sir's departure keeps using the fallback `OpenLevel` — this is the correct degrade, not a bug, and will switch over automatically once 2a lands, no code change needed. |
| 031 | **Organic pacing: `UMelodiaPacingSubsystem` + `UMelodiaPacingProfile`, MelodiaCore-native, scoped to one proof case.** Follows the project's own documented adapter pattern (`MELODIA_SYSTEMS_COMPOSITION_CONTRACT_2026-07-30.md`): stable `FName` ID → `UMelodiaPacingProfile` DataAsset → facade subsystem → typed request → consumer resolves, falling back to its own `EditAnywhere` default on a false return. Migrated exactly two values this pass: `MelodiaSirMelodiousIntroActor`'s `DepartureDelaySeconds`/`DepartureDurationSeconds` (resolved once at the moment each is consumed — the timer set, and `BeginWindowDeparture` respectively — never mutating the authored field itself). | Confirmed by research this session: no "organic pacing" concept existed anywhere in the docs, and every timing value in the project was a scattered local `EditAnywhere` float with no shared authority — a new one does not duplicate anything. Native to MelodiaCore rather than behind the Decision 030 locator: every current consumer (`MelodiaSirMelodiousIntroActor`, `MelodiaBattleSession`, `MelodiaBattleArena`, `MelodiaExplorationActors`) already lives in MelodiaCore, so there is no cross-module problem to solve for pacing specifically. | Deliberately not a project-wide pacing refactor. `MelodiaBattleSession`'s `Pacing` category (`EnemyTelegraphDuration`/`EnemyAttackAnimDuration`/`EnemyPostImpactDuration`), `MelodiaBattleArena`'s hitstop/dolly durations, and `MelodiaExplorationActors.TravelDuration` are follow-up migration candidates, not touched this pass — filed in `_TASK_QUEUE.md`. No `UMelodiaPacingProfile` is authored yet as of 2026-07-31, so every `ResolveDuration` call currently returns false and every consumer sees its own authored fallback — expected, not a bug. |
| 028 | **The `OpenLevel`→`TravelTo` swap covers AUTHORED travel only, not save-restore travel. Option B.** Swap `K2Node_CallFunction_10` (`ChangeMapForBattle`) and `_46` (`ChangeMap`). **Leave `_30` and `_52` — the two `currentMap` save/load legs — on stock `OpenLevel`.** | Cline's live-graph read (233-node `BP_MelodiaJRPGGameInstance` EventGraph) established that all four legs are terminal and data-driven, and the allowlist currently holds two entries. Three reasons B wins: **(1) Domain.** Decision 009 gives the stock JRPG controller save authority. `_30`/`_52` restore a destination from `currentMap` in the save record — gating those behind Melodia's *authored-travel* allowlist makes Melodia's config the gate on stock save-restore, which is a second authority reaching into a domain 009 already assigned. Authored travel (designer picks the destination) and save-restore travel (replaying a state that was already valid) are genuinely different questions. **(2) Failure mode.** `TravelTo` returning false is a silent no-op. Under A, a player hits Continue and the game does nothing — the worst possible failure on the primary re-entry path of a portfolio piece. **(3) A's completeness is false anyway.** MelodiaCore holds seven more direct `OpenLevel` calls that `TravelTo` structurally cannot reach (plugin-vs-game-module dependency; see `_TASK_QUEUE.md`). Choosing A to "finish the job" would not finish it. | This is a scope boundary, not a permanent split. The path to unifying `_30`/`_52`: enumerate every reachable `currentMap` value, allowlist them all, verify no reachable destination hard-fails, *then* swap — filed as its own task, not folded into item 3. **Known cost of B, stated plainly:** save-restore travel does not get spawn-tag placement or the input-context clear that authored travel now gets. That is a real gap, not a free win. Also excluded: Decision 021b (`BP_DefeatDialogue`) is a separate wiring task and must not ride along in the same graph transaction. |
| 029 | **Duplicate-systems audit complete (2026-07-31). Six previously-unaudited domains recorded below. Four are UNRESOLVED and deliberately untouched — do not build on them assuming they are settled.** | Two prior audit attempts died to rate limits, so these domains were never checked and "not flagged" was being misread as "clean". The audit's structural finding explains most of the ambiguity: **the stock TurnBasedJRPGTemplate is a pure-Blueprint stack with no C++ module, while MelodiaCore is a C++ plugin** — so every "Melodia vs stock" pair is *C++ class vs Blueprint asset*, and a C++-only grep always calls the Melodia side live and misses the stock side entirely. | See per-domain rows below. Fixing any of these was explicitly out of scope for the 2026-07-31 unification pass (owner scoped it to trivial wins + documentation) and several collide with Cline's wiring lane and Gemini's UI lane. |
| 029g-RESOLVED | **Reference set enumerated 2026-07-31 via Monolith `project.get_saved_asset_state`. The stop condition does NOT trip — `BP_Melusina` is confirmed legacy and Stage 3 may proceed.** `BP_Melusina` referencers: (1) `/Game/Melodia/_PROJECT/BP_MelodiaGameMode`, (2) `/Game/Characters/Melusina/BP_Melusina` — this is the **redirector artifact** from `DefaultEngine.ini:111`, not a real asset, (3) `/Game/Melodia/UI/WBP_Battle_Rhythm`. `BP_Melusina_BACKUP_20260729` has **zero** referencers — clean to quarantine. | The one that looked alarming was `BP_MelodiaGameMode`, because *its* referencers include a map called `L_MelusinaMorning`. **But there are TWO maps by that name** (see 029h) and it is the stale one. The live route map, `/Game/Melodia/Levels/Opening/L_MelusinaMorning` (modified 2026-07-31 18:15), depends on `/Game/MelodiaIntegration/Blueprints/BP_MelodiaJRPGGameMode` and `BP_MelodiaSirMelodiousMorningIntro` — it has **no dependency on `BP_MelodiaGameMode` and no path to `BP_Melusina` at all**. | Stage 3 order is now determined by the reference set: `WBP_Battle_Rhythm` and `BP_MelodiaGameMode` must be repointed or retired **before** `BP_Melusina` moves, one asset per transaction. The `DefaultEngine.ini:111` `PackageRedirect` must be removed or repointed in the same change. Note `BP_MelodiaGameMode` is itself a 029d duplicate (vs `BP_Melodia_GameMode`) referenced only by the stale map and the four `L_WP_*` sandbox maps — retiring it may be cleaner than repointing it, but that is a separate decision. |
| 029i | **DIAGNOSED 2026-07-31 — "talking to Sir works but never travels to KaleidoNave". The departure is NOT triggered by dialogue. It has no connection to QuillScript whatsoever.** The real chain (`MelodiaSirMelodiousIntroActor.cpp`): player pawn overlaps Sir's `ReunionTrigger` **sphere** → `HandleReunionOverlap` (`:87`) → iterate to the first `AMelodiaOpeningStateAnchor` and read its `ResonanceBond` (`:101-103`) → set `bReunionTriggered` → `DepartureDelaySeconds` timer (`:110`) → `BeginWindowDeparture` (`:157`) → gate `if (!Flow \|\| !Flow->NotifySirDeparted()) return false;` (`:165`) → animate `DepartureDurationSeconds` → `OpenLevel(DepartureDestinationLevel)` (`:205`). | **Every static gate was verified correct in the live level**, so this is a runtime/authoring issue, not a config one: `bEnableReunionBeat = true` and `bDepartAfterReunion = true` (C++ defaults, `.h:50,54`); `Morning_OpeningState` anchor exists with a valid `ResonanceBond`; `BeginMorning()` IS called in `BeginPlay` (`:83`) so the `NotStarted→Morning` transition that `NotifySirDeparted` requires should hold; and Sir's `DepartureDestinationLevel` is **already** `/Game/EnvSandbox/Environments/L_KaleidoNave`. Most likely cause: **the player talks to Sir without ever entering the `ReunionTrigger` sphere** — dialogue and departure are separate systems with separate radii. Second candidate: a save restore wins the race against `BeginMorning()` (the hazard `MelodiaSaveGameSubsystem.h:70` explicitly warns about), leaving the phase ≠ `Morning` so `NotifySirDeparted()` returns false and `BeginWindowDeparture` aborts **silently at `:165` with no log**. | **Separate, definite bug found alongside it:** the `Morning_Portal_Dreamstate` actor (`MelodiaOpeningPortal_0`) still has `DestinationLevelName = /Game/Melodia/Levels/Opening/L_Melodia_Dreamstate` — the standalone map that was merged into KaleidoNave. Sir's destination was updated; the portal's was not. **Two fixes, neither done:** (1) repoint the portal to `L_KaleidoNave`; (2) add a `UE_LOG` on the `:165` early-return — a silent abort on the route's only departure path is why this cost an afternoon to find. Note `MelodiaOpeningPortal.cpp:45` is also one of the seven MelodiaCore direct `OpenLevel` calls that `UMelodiaTravelSubsystem` structurally cannot reach. |
| 029h | **NEW DUPLICATE FOUND 2026-07-31: there are two `L_MelusinaMorning` maps, and one silently preserves the retired GameMode chain.** Live: `/Game/Melodia/Levels/Opening/L_MelusinaMorning` (modified 2026-07-31 18:15, uses `BP_MelodiaJRPGGameMode`, matches the path in `_VERTICAL_SLICE_SCOPE.md`). Stale: `/Game/L_MelusinaMorning` at Content root (last indexed 2026-07-26, references `BP_MelodiaGameMode` → `BP_Melusina`). | Not in the 029d duplicate list because nothing had enumerated it. It matters more than the other duplicates: it is a copy of the **route's first map**, and it is the sole thing keeping the legacy GameMode → legacy pawn chain reachable. Anyone opening the wrong one gets a working-looking morning scene wired to retired systems. | Do not delete on this evidence alone — confirm in-editor which one the route actually loads, then quarantine the other (mirrored path, reversible, per Decision 022). Add to the 029d duplicate-asset list. Same caution as the rest of 029d: `.umap` files reference things a C++ grep calls dead. |
| 029g | **Two live Melusina pawns with two wardrobe systems — decided direction, execution BLOCKED.** `BP_Melusina` (`Content/Melodia/Characters/Melusina/`, parent `MelodiaSmokeCharacter`, carries `UMelodiaOutfitComponent` for garment meshes) vs `BP_MelusinaJRPGCharacter` (`Content/MelodiaIntegration/Blueprints/`, parent stock `BP_JRPGCharacterBase`, uses stock `BP_EquipmentBase` for stats + battle meshes). **An outfit change in exploration does not follow into battle** — they are different pawn assets. Persona adds a *third* equipment vocabulary (`MelodiaPersonaTypes.h:54-59,140-141` `FMelodiaEquipmentDefinition`). | **Direction decided (owner, 2026-07-31): consolidate onto `BP_MelusinaJRPGCharacter`, retire `BP_Melusina`.** That pawn is provably live — it is `BP_MelodiaJRPGGameMode`'s default pawn (and that GameMode is `GlobalDefaultGameMode`), and PIE logs it: `MELUSINA_HAIR_BOUND owner=BP_MelusinaJRPGCharacter_C_1 … attach_bone=head_x`. The hair fix landed on it. Decision 009 also requires the battle pawn stay a stock `BP_JRPGCharacterBase` child, so merging the other direction is not available. **Bonus:** `BP_Melusina.uasset` → `MelodiaOutfitComponent` is one of Decision 021's three original leaks, so retiring it closes that entry. | **BLOCKED, not done.** Confirmed: zero `.umap` references to `BP_Melusina` (not placed in any level). **Unconfirmed: `.uasset` references** — binary name tables do not yield to grep, and a full-Content sweep times out. Needs the editor open + Monolith's asset-reference query. Also outstanding: `Config/DefaultEngine.ini:111` carries a `PackageRedirect` for `BP_Melusina` that must be removed or repointed in the same change, or a stale reference will silently resolve to a quarantined asset. **Stop condition: if a live-route map turns out to reference it, the "legacy pawn" premise is wrong and the direction needs rethinking before any asset moves.** |
| 029a | **GameMode ownership is UNRESOLVED — the GameMode that would set the HUD is not the one that runs.** `Config/DefaultEngine.ini:13` boots `L_MelodiaMainMenu` (→ `AOrreryMainMenuGameMode`); `:15` sets `GlobalDefaultGameMode` to `BP_MelodiaJRPGGameMode`, whose parent is plain **`/Script/Engine.GameModeBase`, not `AMelodiaGameMode`**; and `AMelodiaGameMode` is referenced only by the four `L_WP_*` sandbox maps, `L_MelusinaMorning.umap`, and **two separate GameMode BPs off the same parent** (`BP_Melodia_GameMode`, `BP_MelodiaGameMode`). `AMelodiaMobileGameMode` has **zero** asset references — dead — yet `UMelodiaMobileHUD` is live-via-asset (`WBP_Battle_Mobile.uasset` reparents to it), so the widget exists and its GameMode never runs. | This is the root cause of the HUD-ownership ambiguity, not a separate problem. | **Do not build HUD or GameMode-owned behaviour on top of this until it is resolved.** Resolution is its own pass. |
| 029b | **Camera has no view-target arbiter.** `MelodiaPartySubsystem.cpp:151-154` (`SetViewTargetWithBlend`) is the **only** `SetViewTarget*` caller in the entire C++ codebase. `AMelodiaBattleArena` (SpringArm + Camera + shake, `MelodiaBattleArena.cpp:30-35,150-156`), `AMelodiaCharacterBase`/`AMelodiaSmokeCharacter` boom cameras, and the stock `BP_Camera` (placed in `MelodiaIntegrationMap.umap`, driven by `BP_BattleBase`/`BP_BattleController`) all act independently and unaware of each other. | No PlayerCameraManager subclass, no blend arbiter. Party-swap blends and battle-arena takeover can fight. | Pick one view-target authority in a later pass. Not urgent — it has not visibly broken — but it will, and it should be a deliberate choice rather than whoever calls last. |
| 029c | **UI architecture is "Melodia skin over stock JRPG", and that is intended.** `Content/MelodiaIntegration/UI/BP_Melodia*UI` each **import and derive from** `/Game/TurnBasedJRPGTemplate/Blueprints/UI/BP_BattleUI`, `BP_ActionsUI`, `BP_TurnOrderUI` etc. They are skins, not replacements. | Stated explicitly because the relationship is easy to mistake for duplication and "fix" by forking — which would create the second authority Decision 009 exists to prevent. | Do not fork the stock widgets. Extend by derivation. Separately: the stock `BP_InteractionDetector` (canonical, referenced by the live PlayerController/GameInstance and every interactable) does **not** consult `UMelodiaInputContextSubsystem::IsInteractionAllowed()` — it should; filed as follow-up. |
| 029d | **Duplicate assets requiring in-editor verification of which one the live map loads:** `WBP_RhythmHUD` ×3 (`Content/Blueprints/`, `Content/Melodia/Blueprints/`, `Content/_PROJECT/Blueprints/Gameplay/BP_RhythmHUD`), `WBP_Settings` vs `WBP_MelodiaSettings`, `WBP_SaveLoad` vs `WBP_SaveLoadPanel`, `BP_Melodia_GameMode` vs `BP_MelodiaGameMode`, `BP_Melusina_BACKUP_20260729`. `UMelodiaSettingsPanelWidget` (game module) is a *third* settings path alongside the two settings widgets. | Cannot be resolved by static inspection — needs the editor to see which asset the running map actually loads. | Do not delete any of these on grep evidence alone. Decision 020's rule stands: content references what C++ greps call dead. |
| 029e | **Audio is already single-owner; the MetaSound concern was unfounded.** `UMelodiaAudioComponent` is the sole BGM owner (`PlayBGM`/`StopBGM`/`PlayBGMQuantized`, `MelodiaAudioComponent.h:36/40/48`), driven only by `MelodiaBattleSession.cpp:438/442/1299`. `UMelodiaMusicClockSubsystem` and the audio-reactive subsystem are **consumers** of its Quartz clock — correct layering. Stock JRPG ships SFX cues only, no competing BGM. No Melodia MetaSound exists (the only MetaSounds are third-party `UltraDynamicSky`). | Recorded so this domain is not re-audited. | **One known bypass, deliberately left:** `OrreryMainMenuGameMode.cpp:244` raw-`NewObject`s a `UAudioComponent` for menu ambience, so it ignores `BGMVolume`/`StopBGM`. Unifying it is **not trivial** — `PlayBGM(FName)` is skill-keyed and path-convention-based (`/Game/Audio/BGM/BGM_<SkillId>`) and cannot play an arbitrary `USoundBase`. It needs a `PlayBGMSound(USoundBase*)` primitive extracted from `PlayBGM`, which touches the component battle audio depends on. Deferred rather than done mid-flight. |
| 029f | **There is no level-streaming competitor because there is no streaming at all.** `LoadStreamLevel`/`UnloadStreamLevel` count across `Source/` and `Plugins/MelodiaCore/Source/`: **zero**. `WorldPartition`/`DataLayer` C++ references: **zero**. Everything is hard `OpenLevel`. `UMelodiaMapTransitionComponent` already delegates to `TravelTo` and is no longer a competitor. | Nothing to unify here today. | **But:** the four `Content/EnvSandbox/Environments/WP/L_WP_*.umap` maps *are* World Partition and all reference `AMelodiaGameMode`. If they ever become the shipping world, `OpenLevel`-only travel is incompatible with a streamed open world. Flag now, decide if/when those maps enter the route. |
| 032 | **NEW DUPLICATE FOUND 2026-07-31: the ~64 custom UI textures (`T_Melodia_*`, `SoftMG_*`) used for JRPG UI theming exist duplicated with identical names across three Content locations.** `/Game/EnvSandbox/Textures/Melodia/GameUI/`, `/Game/EnvSandbox/Textures/Source/MelodiaGameUI/`, and `/Game/EnvSandbox/Alphas_Melodia/` (partial overlap only — e.g. `T_Melodia_NoteHead`, `T_Melodia_Hitline`, `T_Melodia_FiligreeCorner`, `T_Melodia_Grain`, `T_Melodia_StaffTile`, `T_Melodia_FiligreeDivider`, `T_Melodia_SheenSweep`). Found via a Monolith `project_query("search")` text search, not a full reference audit. | Same shape as Decision 029d, one level down — texture assets instead of widgets/blueprints. No canonical copy has been determined. | **Unresolved. No asset touched or deleted.** Cannot be resolved by static inspection — needs an in-editor Monolith `find_references`/`get_saved_asset_state` pass to determine which copy live widgets actually reference before any consolidation or quarantine. Add to the 029d duplicate-asset list scope. |
| 027 | **UEBlueprintMCP (github.com/lilklon/UEBlueprintMCP) installed at `Plugins/UEBlueprintMCP`, registered in `.mcp.json` as `ueblueprintmcp`, but NOT added to `enabledMcpjsonServers`.** Available on demand, not live by default. | The owner asked for it explicitly for autonomous Blueprint-wiring work. But it's a 9-commit, 36-star third-party plugin with no audit history, and its ~60 commands (Blueprint graph nodes, components, materials, UMG, Enhanced Input) substantially overlap Monolith and VibeUE, which are both already live. Registering-but-not-enabling gets the explicit ask (available, one line from live) without a third unaudited authority fighting the other two over the same graph by default — the same reasoning Decision 025 already applied to VibeUE, this time actually followed through rather than reversed. | Enable it deliberately per task, not project-wide, when its Enhanced Input command set (Input Actions / Mapping Contexts) covers a gap. Check first whether `set_node_property` (Decision 024) already closes that gap before reaching for a third surface. Requires a closed-editor `Build.bat` pass before first use — it's a new plugin module with `UCLASS`/`GENERATED_BODY`, so Live Coding cannot register it. |
| 033 | **Psych/music + psychological-horror indie research is locked to docs and mapped to existing Melodia systems, not to new mechanics.** `Docs/Research/MELODIA_PSYCH_MUSIC_INDIE_REFERENCE_2026-07-31.md` records OMORI, Crypt of the NecroDancer, Undertale, Sayonara Wild Hearts, Wandersong, Silent Hill 2, Amnesia, SOMA, Layers of Fear, DDLC, Pathologic and more — each with a per-entry "What Melodia takes / What Melodia rejects" verdict that lands on systems that already exist (Resonance buff, QuillScript allowlisted intents, `IsGatedContentAvailable`, `UMelodiaMusicClockSubsystem`, `UMelodiaPacingSubsystem`, travel authority, dream-state prologue). | The project had no researched design reference; the "music you cannot fail at" thesis and the OMORI-informed Decisions 016/017 were already in the code but never traced back to their sources. Locking the research now gives every future agent a single, cited reference so a decision doesn't get re-derived differently each session — the same drift the dated-doc rule exists to prevent. Horror is integrated as *tonal register* on existing systems (dream-state decay, silence, pacing stutter, unreliable anchors) and as *reference vocabulary* (Amnesia's sanity as placebo, not meter; Silent Hill's fog-as-visibility; Layers of Fear's environment-as-unreliable-narrator), never as new mechanics. | Not a mandate to add horror mechanics, jump scares, sanity meters, or any new system. The doc is reference material, not scope. **No MelodiaCore/BS_GodFile code was changed for this decision** — it is documentation plus a cross-link from `Docs/MELODIA_IDENTITY_AND_LOOP_2026-07-30.md`. |
| 036 | **The author's lived material (grief, lived abandonment, BPD, OCD, isolation, feeling-behind) is the narrative core of Melodia, mapped metaphorically, and locked to docs.** `Docs/Research/MELODIA_BARD_GRIEF_HOOK_2026-07-31.md` converges on one hook: Melusina the travelling bard keeps arriving late (post-festival), the past duet-partner who left her is the heavy wound (fragments, never a corpse), Sir Melodious is **alive and retrievable** (flew off for snacks — a benign absence the dream catastrophizes), and the ending register is **reunion**. The Resonance verb *is* the metaphor (a mark set, an answer awaited). Explicitness balance: feel-first (Gris model) for ~90% of the game, **named once** at the reunion in one human sentence, never as diagnosis. | The owner's ground truth (2026-07-31) is that the psych/music and OMORI work is only the vocabulary — the *content* must be their own story, not OMORI's guilt-wound. Mapping each lived theme to an existing system (dual-world palette split = BPD splitting; the opening fizzle + held-beat pacing = OCD as ritual not punishment; arrival-after-festival = feeling behind; Resonance = "will anyone answer my song?") keeps the hook Melodia-native with zero new mechanics, consistent with Decision 033. | **No code changed for this decision.** Non-negotiables recorded: no dead Sir / animal harm; no guilt-wound (Melusina is survivor, not perpetrator); no sanity/dream-integrity meter with teeth; OCD-as-failure is a Decision 016 violation; no on-screen diagnosis; no "no-easy-catharsis" mandate — reunion is the ending, full stop. This is reference/authoring guidance, not a change request. |
| 037 | **PPV / toon-outline lane split and foliage treatment, settled 2026-08-01.** (a) **Lane split while multiple agents work the same look:** Claude owns PPV attachment, blendable stacks, and material-instance parameters; Codex owns the outline *algorithm* (Custom HLSL). Neither edits the other's surface in the same session. (b) **Foliage ink is handled by parameter tuning on an instance, not by a new mechanism.** `MI_PP_StorybookOutline` carries `FalloffStart=1200` and `NormalWeight=0.5`; the master keeps its defaults. (c) **PPV blendables are attached via material *instances*, never the master** — `setup_nikki_render_post_process.py` resolves a per-role preference list, instance first, master as fallback. | (a) Two agents editing one Custom HLSL node is how `M_PP_StorybookOutline_LookdevCandidate` ended up a byte-identical duplicate of work described as a rewrite — the split makes that failure impossible rather than merely discouraged. (b) `falloff` only attenuates geometry *beyond* `FalloffStart`, and grass fires *both* edge terms (the depth threshold is 1% of view distance, which blade spacing exceeds), so distance alone buys grass relief only by also killing distant architecture outlines; trimming `NormalWeight` targets the actual blowout. Both are existing parameters — no new dial was invented to cancel out a problem. (c) Tuning the master forces a shader recompile on a shared asset, cannot vary per level, and collides with whoever is editing the graph. | Does **not** promote `M_PP_StorybookOutline_MathCandidate_v2` — that is a separate owner-approved step after A/B. Does **not** cover the vine overlay or `UDSTintStrength`, both still 0/untuned. Does **not** fix the stale quarantined `PostProcessVolume` found in `L_SakuraPath` and `L_Template` (see below) — those are owner calls. **Operational lesson recorded (corrected 12:21):** a buffer/view-geometry bug is invisible whenever `BufferSize` and `ViewSize` happen to match, so **"it looks fine now" is not evidence of a fix** — an editor restart merely realigns them and masks the bug. Verify by *deliberately* forcing a mismatch: capture at a resolution different from the viewport, which is also the condition portfolio renders run under. This exact claim was asserted and retracted twice in one session (first "motion-only, not the shader", then "resolved by restart"), both times from looking at a screen instead of forcing the failure. Engine-side capture via `AutomationLibrary.take_high_res_screenshot` is the tool that settles it — and it works, provided nothing sleeps on the game thread after the call. |
| 037a | **UNRESOLVED, owner call — stale quarantined post-process volumes are live in two levels.** `L_SakuraPath` and `Content/EnvSandbox/_Template/L_Template` each contain an unlabeled, enabled, unbound `PostProcessVolume` at priority 0 still carrying the quarantined `M_PP_ToonOutline` + `M_PP_StorybookVines_Inst`. In `L_SakuraPath` it coexists with `PPV_NikkiDream`, and because weighted blendables **accumulate** across overlapping volumes, that level renders the old outline stacked on the new one. | The two materials were quarantined on 2026-08-01 after "zero referencers confirmed first" — but that check looked at asset referencers, and these are `.umap` *actor* references, which do not surface the same way. Same class of blind spot as Decision 020/029d: content references what a reference check calls dead. | **Not fixed by an agent.** `L_SakuraPath` art direction is human-owned (standing rule), and `L_Template` propagates to every level copied from it, so both are deliberate owner decisions. Fix is to delete or disable the unlabeled volume in each. Until then, do not judge `L_SakuraPath`'s outline look — it is showing two stacked passes. |
| 043 | **Re-open the wardrobe lane in additive, non-gameplay-gating form, 2026-08-07.** New `Plugins/MelodiaWardrobe/` plugin hosts the universal outfit collection + fashion gameplay system. Slot-swap engine + cosmetic registry + wardrobe state + gacha + UI. First pass is `Cos_Dress_Melusina` end-to-end; bulk-register of the other 38 cosmetics is gated on the first passing. `FMelodiaNarrativeRecord` bumps v2 → v3 with three additive SaveGame fields (`OwnedCosmeticIds`, `EquippedCosmeticIds`, `LastPullUnixSeconds`) — no schema break, v2 saves load cleanly. Plan doc: `Docs/MELODIA_WARDROBE_PLUGIN_PLAN_2026-08-07.md`. | The foundation closeout §2.2 ("Take the gating philosophy; leave the wardrobe deferred") explicitly deferred the *gameplay* axis (outfit-ability soft gates). The collection/UI/commerce axis — pulling a dress, equipping it, owning the wardrobe — was always the safe half; the only reason it was bundled with the deferral is that the old `UMelodiaOutfitComponent` lived in quarantined `MelodiaCore`, where any use re-opens the plugin as a runtime. The new plugin host separates the algorithm from the quarantined runtime, so the safe half can ship without re-opening the unsafe half. The project is in a healthy state (PIE-verified First Dream route, 46/3 test baseline, save round-trip working) and can afford a narrow additive lane. | **The "outfit-ability" gameplay axis stays deferred.** This PR ships collection, equip, save/load, and gacha. It does not ship "wear this outfit to climb the vine." Any gameplay-gating use of the wardrobe requires a new decision. The other 38 cosmetics are not bulk-registered in this PR — gate 10 of the plan says stop after one dress and owner-review before continuing. |
| 044 | **Re-host the `UMelodiaOutfitComponent` algorithm into the new `MelodiaWardrobe` plugin, 2026-08-07.** The slot-swap algorithm at `Plugins/MelodiaCore/Source/MelodiaCore/MelodiaOutfitComponent.cpp:16-58` (`FindOrCreateSlotComponent` + `EquipGarment`) is copied into `MelodiaWardrobe/Source/Private/MelodiaWardrobeComponent.cpp` with three additions: material override, mirror to `UMelodiaWardrobeSubsystem`, type rename. The quarantined source remains on disk untouched. Decision 3.1 (move MelodiaCore's 65 dead headers to `_Reference/`) is the right cleanup of the now-redundant `UMelodiaOutfitComponent`; it is a separate decision, not this one. | Decision 020/034 quarantined the `MelodiaCore` *plugin* as a runtime authority, not the individual algorithms inside it. The slot-swap algorithm is sound — its parent pawn `BP_Melusina` was retired for being a second authority, not because the component was wrong. Copying the algorithm into a bounded new plugin honors the quarantine (the quarantined plugin remains untouched, still does nothing) and is strictly cheaper than re-deriving it from the 39 cosmetic JSON drafts. | The re-host is one-way: do not move, delete, or modify the original quarantined source. If the original is later moved to `_Reference/` per Decision 3.1, the copy in the new plugin stands. |
| 045 | **Marketplace plugin import closed for this wardrobe PR, 2026-08-07.** `uDress` and other UE Marketplace wardrobe plugins (Modular Character System, Skyfall Character & Outfit, etc.) and the corresponding gacha/shop candidates are NOT imported. Re-host from quarantined `MelodiaCore` + hand-rolled subsystems are the entire wardrobe engine; hand-rolled gacha (`UMelodiaWardrobeGachaSubsystem`, ~120 lines) replaces every gacha plugin on the grounds that all candidates bring their own currency store, which conflicts with the project's single `UMelodiaTokenWalletSubsystem` authority (Decision 020/029g). | Marketplace wardrobe plugins carry a 5.8 build-verification cost and a license review cost, and the closest fit (`uDress`) is not verified on UE 5.8 as of 2026-08-07. Gacha/shop plugins universally bring their own currency, which is the exact second-authority hazard that quarantined `MelodiaCore`. The re-host + hand-roll path is zero-cost, zero-license, and uses an algorithm we already own. | No paid plugin spend in this PR. If a future decision reopens the question, a fresh compat lab (`CompatibilityLabs/MelodiaWardrobeUE58/`, mirroring the `TurnBasedJRPGUE58` pattern) would precede any import. |

## 2026-08-08

| # | Decision | Rationale | Excludes |
|---|---|---|---|
| 046 | **T3D baseline + patterns tooling committed under tracked `Docs/`, not `Saved/`.** `Docs/T3D_Baseline/` (55 hash-verified exports of the toon/material spine, `verify_baseline.py` drift gate) and `Docs/T3D_Patterns/` (5 parameterised snippets + `t3d.py` CLI) are the canonical location for T3D catalogs going forward. Monolith gained `blueprint/inject_nodes_t3d` and `blueprint/validate_nodes_t3d` (1328 → 1330 actions), documented in `Plugins/Monolith/Docs/MONOLITH_GUIDE.md` Recipe 16 and `Docs/specs/SPEC_MonolithBlueprint.md`. | Every earlier T3D catalog was an untracked one-off because it landed under `Saved/`, which is gitignored — it evaporated on the next clean checkout and had to be redone. Putting the baseline under tracked `Docs/` fixes the root cause once, rather than repeating the export each session. | `export_asset_text` still silently truncates over its 256 KiB default budget while reporting success (~166-byte plain-text notice instead of an error) — this produced the bogus 171-byte `BP_QuestNotification` stub in the 08-05 catalog. Any future caller of this action must assert `full_bytes == returned_bytes`, not just check for a success flag. |
| 047 | **TouchDesigner OSC bridge fixed both directions, 2026-08-07/08.** TD→UE (port 8000): three defects fixed in `Content/Python/osc_server.py` — MPC writes staged and flushed on the game thread via a Slate post-tick callback (were silently no-op'ing from the OSC listener's daemon thread); `_get_mpc` corrected to resolve the asset, not the Package; `MaterialLibrary` used instead of `MaterialEditingLibrary` for parameter-collection writes. UE→TD (port 9000): two real edges wired into the previously-dead `UMelodiaRhythmReactivitySubsystem::Notify*()` broadcast — grade via `MelodiaJRPGPresentationRhythmComponent::HandleRhythmSessionCompleted`, beat via `MelodiaAudioReactivePresentationSubsystem::TickPresentation`, plus a 2 Hz idle heartbeat. | The bridge had never worked in either direction despite being documented as live. TD→UE failed because every write happened off the game thread; UE→TD failed because every caller of the notify functions lived in quarantined MelodiaCore classes nothing instantiates, so the socket was structurally dead regardless of the OSC code's own correctness. Verified: 6/6 MPC params move live with TD audio; 73 UE→TD datagrams across all six `/rhythm/*` addresses in an 8s PIE window. | `deploy/touchdesigner_validator.py`'s `PROJECT_ROOT` (wrongly pointed at `G:`, the UE project's drive, not the TD project) was the one genuine bug in the path config — the `G:\` paths in `td_bridge.py` and `deploy/start_*.ps1` are correct and must not be "fixed" back to `C:`. TD only cooks while its timeline is playing and its window is not minimised; this is an operating constraint, not a bug to chase. |
| 048 | **`Dreamstate_WakePortal` is one actor, not two — retracting the earlier "portal in each level" framing.** `L_KaleidoNave` streams `L_Melodia_Dreamstate` as a sublevel, so the single portal actor was being observed from both the container level and its streamed sublevel and misread as two separate portals in two separate levels. `DestinationLevelName` fix (B5, `/Game/ZenForestTest` → `L_KaleidoNave`) stands and is reload-verified. | Chasing a second, non-existent portal actor would have wasted a future session. Recording the correction here so it isn't rediscovered from scratch. | Open risk carried forward, not resolved: the portal now targets a level that streams the level it lives in — a re-entry loop is possible and has not been ruled out by a live traversal test. |
| 049 | **`MelodiaQuillTwilightDancer` and its four sibling Quill assets are NOT orphaned — retracting the earlier orphan-script finding.** `QuillDialogue` is declared as a `TSoftObjectPtr`, so asset-registry `referenced_by` queries return `[]` for all five Quill assets by design (soft references don't populate the hard-reference graph) — this is expected tooling behavior, not evidence of disuse. All five are confirmed wired in `ZenForestTest.umap`. | A prior session's asset-reference audit treated the empty `referenced_by` list as proof of an orphan and flagged it for possible quarantine. That would have been a real content-loss risk against live-wired dialogue. | Do not re-run an orphan check against these five assets using hard-reference tooling — it will always read empty for a `TSoftObjectPtr` field, regardless of actual wiring. If a future orphan sweep needs to cover soft references, it needs a different query, not a repeat of this one. |

---

## 2026-08-14

| # | Decision | Rationale | Excludes |
|---|---|---|---|
| 050 | **Bedrock is live via `bedrock-mantle`, not the account root/IAM-user data-plane path recorded as blocked on 2026-08-13.** `bedrock-mantle` is the model lane the router now uses to reach Amazon Bedrock. | The 08-13 handoff recorded every model call returning `Operation not allowed` for both root and the IAM user, blocked on an unrecorded account-level use-case form. `bedrock-mantle` resolves that path without waiting on the form. | Does not retroactively mean the use-case-form blocker was fixed by AWS — `bedrock-mantle` is the lane that works regardless of that form's state. Do not re-litigate the form-vs-support-ticket question already recorded 2026-08-13. |
| 051 | **`qwen.qwen3-coder-next` is the default model for the Bedrock lane.** | Selected as the working default once `bedrock-mantle` made Bedrock calls succeed; supersedes the router's earlier fallback chain that never reached Bedrock in practice. | Not a claim that other models (Claude via Bedrock, Nova, etc.) are unavailable — only that this is the default when no other model is specified. |
| 052 | **S3 art drop is the collaborator asset-delivery mechanism**, replacing "ask the owner" onboarding for bulk environment art. | The `_TASK_QUEUE.md` "AWS S3 Art-Drop Mechanism" row (Sync/P1) called for exactly this: a bucket for the 4.6 GB bulk EnvSandbox art so new collaborators/agents don't depend on a manual owner hand-off. | Distinct from the existing Glacier Deep Archive backup bucket (`s3://melodia-archive-322037002075/unversioned-art/`), which is a disaster-recovery cold store, not a working delivery mechanism — the two buckets serve different purposes and are not interchangeable. |

---

## Decisions Made Before 2026-07-26 (Historical — Reference Only)

These are recorded in the intake report (`_INTAKE_REPORT_2026-07-26.md`) and various prior docs. They are not re-litigated:

- 7 blessed master materials architecture (locked 2026-07-07)
- 4 World Portrait levels as primary showcases (locked 2026-07-07)
- `L_SakuraPath` art direction is human-owned (standing rule)
- No force-push of `main` (244-commit local/remote divergence)
- Multi-agent system design decisions (superseded by Decision 002 above)

## 2026-08-18

| # | Decision | Rationale | Excludes |
|---|---|---|---|
| 043 | **Project-wide orchestration unifications.** We formally adopt a Unified Event Bus for subsystem decoupling, externalized data authority (DataAssets/JSON instead of Blueprint nodes), a strict global taxonomy for assets, standardized graceful degradation protocols for disconnected external services, and a strict swarm state initialization contract (_SESSION_HANDOFF.md) to prevent agent context stomping. | Prevents subsystems and subagents from fighting over authority. Resolves friction between Unreal systems (UI/Rhythm/Combat) and agent operations. | Custom workarounds, Blueprint-native logic constants, direct component dependencies. |
| 044b | **KawaiiPhysics replaces Chaos Cloth for stylized anime characters.** The Japanese TA plugin pafuhana1213/KawaiiPhysics is adopted for Melusina's hair, skirt, and ribbons. | Extremely lightweight, AnimNode-driven, preserves volume perfectly (no stretching), and integrates with our audio-reactive needs without the CPU/GPU overhead of native Chaos. | PhysX/Chaos cloth solutions. |
| 045 | **Substrate Toon BSDF + MooaToon/UE_CelLit adopted for premium rendering.** Integrating UE 5.8 native Substrate Toon shaders alongside MooaToon and UE_CelLit (for SDF face shadows). | Fulfills the Infinity Nikki lookbook requirements, guaranteeing top 0.1% UE workflow fidelity for JRPG aesthetic. | Legacy post-process cell-shading hacks. |
| 046 | **Project Health Dashboard presentation layer.** Rebuilt dashboarding to natively render the echo pipeline, MCP policies, and ledger records via project_health_dashboard.py within the ebuild_all_dashboards.py orchestration. | Unifies the project's SSOT into a single human-readable telemetry HTML page without conflicting with the programmatic gate. | Over-verifying or adding competing gate mechanisms. |

---

## 2026-09-05

| # | Decision | Rationale | Excludes |
|---|---|---|---|
| 053 | **Four live JRPG battle-trigger bugs found and fixed in `MelodiaIntegrationMap` during a 5-iteration live-PIE test pass.** `BP_InteractionBattle2`/`3`/`4` had empty `enemyList` arrays — walking in soft-locked the player in battle mode permanently (no enemy ever satisfies the win condition, `isExplore`/`isInputBlocked` never release). `BP_PermanentBattle2`/`3` each had a null-enemy-class row in their weighted spawn table (25-50% roll chance) — a live spawn roll onto that row would attempt to spawn a null class. All four fixed by copying the known-good `BP_InteractionBattle`'s `enemyList` (`BP_WeakEnemy`, spawnChance 1) into the empty ones, and by deleting the null-keyed rows from the two spawn tables (kept their real entries intact). Re-verified live: `BP_InteractionBattle3` starts a real battle and hands the turn to the player unit; `BP_PermanentBattle2`'s spawn table now only ever rolls real enemy classes. | Stock JRPG template content (`Content/TurnBasedJRPGTemplate/...`) ships with unconfigured/partially-configured trigger instances placed in this test level; nobody had walked into most of them under live PIE before. | Other placed trigger instances in other levels were not audited — this pass covered only the 8 battle-trigger actors in `MelodiaIntegrationMap`. |
| 054 | **Root-caused and fixed a real editor crash: `AnimNode_SequencePlayer.cpp:92` ensure escalating to a fatal assert — a Sequence Player node cannot play an `AnimMontage`.** `BP_SirMelodiousPlayerUnit`'s `dieAnimation` (typed `AnimSequenceBase`, feeds `A_BattleAnimationBlueprint`'s Sequence Player directly, no Slot/Montage_Play involved) was set to `AM_Melusina_Death`, an actual `AnimMontage` — the instant `isDead` flips true in battle, this crashes the editor. Fixed by pointing `dieAnimation` at a genuine `AnimSequence` (later superseded by Decision 056's mesh/anim rework). | Found while live-testing Decision 053's battle triggers — the crash was real and reproducible, confirmed via the crash dump's `AnimationContext.txt` and the `EnsureFailed` log line naming the exact node and condition. | Does not mean every `*AnimationMontage`-suffixed property on unit classes is safe — only `idleAnimation`/`dieAnimation` (the two `AnimSequenceBase`-typed, non-Slot-routed variables) were audited. |
| 055 | **Two `.uasset` files were stuck read-only and crashing Claireon's periodic autosave every ~4 minutes, independent of any active task — cleared, not investigated further.** `Content/EnvSandbox/Materials/Masters/M_Master_Nikki_Landscape.uasset` and `Content/EnvSandbox/Materials/Functions/MF_Triplanar_LandscapePro.uasset` both carried the Windows read-only attribute (`attrib` showed `R`) while sitting dirty in the working tree; Claireon's autosave (`AutoSaveDebounceSeconds`) tried to write them on a timer and hit a fatal `Assert` on the denied write, three times in one session before being caught. `attrib -R` on both — no asset content touched. | This was actively crash-looping the editor regardless of what else was being worked on; per this project's own `p0-loop` skill, a read-only `.uasset` after checkout is a known, expected, fix-it-yourself trap, not a content decision needing sign-off. | Not a full sweep — only these two files (the ones that actually crashed) were checked and cleared. Other dirty files under `Content/EnvSandbox/Materials/` were not individually verified read-only or not. |
| 056 | **Melusina's battle-side unit (`BP_SirMelodiousPlayerUnit`) now renders as herself, not the stock `SK_Mannequin` placeholder — new `ABP_Melusina_Battle` + a `BeginPlay` override, live-verified.** Root cause: the JRPG template's mesh/AnimClass assignment is native, closed-source, and `Transient` on the `SkeletalMesh` component (confirmed via `bp_get_component_details` — no Blueprint graph anywhere in the unit hierarchy sets it). Fix is additive: `BeginPlay` now calls `SetSkeletalMeshAsset(SK_Melusina_V2_Body)` + `SetAnimInstanceClass(ABP_Melusina_Battle)` on the component after native spawn logic runs. `ABP_Melusina_Battle` mirrors the stock `A_BattleAnimationBlueprint`'s 8-node graph exactly (compiles with 0 warnings) but targets `SK_Melusina_Skeleton`. **First attempt used the wrong assets** (`SK_Melusina` — a legacy single mesh, not the real V2 body — plus `QuaterniusRetargeted` idle/death sequences) and produced a badly broken, twisted pose; corrected per the owner's direction to use `SK_Melusina_V2_Body` (the mesh the working exploration character `ABP_Melusina_Current` actually uses) and `A_Melusina_Idle_Mocap_RootX` (authored mocap, not a retarget) — live-reverified, pose is now anatomically correct. | The owner identified mid-session that the `QuaterniusRetargeted` animation suite is a known source of major pose regressions and that `SK_Melusina_V2_Body` + the authored mocap suite is the correct asset family going forward. | **Two open risks carried forward, not resolved:** (1) a flipbook material is rendering as a large floating slab near her head in battle — owner confirmed this is a known-needs-work material issue, out of scope this session. (2) `dieAnimation` still points at `A_Q_Melusina_Death01` (the same `QuaterniusRetargeted` folder implicated in the idle regression) because no mocap-sourced death sequence exists — likely broken the same way; not yet tested since it only plays on `isDead=true`. Wardrobe pieces (Shirt/Skirt/Boots/Accessories) are not attached to the battle unit — she renders as the bare V2 body, not fully dressed. |
