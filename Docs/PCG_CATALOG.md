# PCG Library Catalog — RETIRED 2026-08-05

> **This file is retired.** It was flagged stale on 2026-07-24 (references a renamed/reorganized graph
> library) and its regeneration was queued in `Docs/QUEUE.md` (Phase 3). The content below is kept as
> read-only history; do not treat the counts or paths as current.
>
> **Current authority:** the live PCG library under `/Game/EnvSandbox/PCG/` (in-editor `Content Browser`
> view or `monolith` asset queries). Naming decision still open (owner's call): `PCG_RockScatter` vs
> `PCG_Universal_RockScatter` — not a duplicate, deliberate succession.
>
> If this catalog is ever regenerated, do it from the live tree (e.g. via `assign_room_pcg_graphs.py`
> conventions) and write a NEW dated file; do not restore this one.

*(The stale 2026-07-26 catalog body was discarded in the 2026-08-05 retirement; nothing below here.)*