# Deep Review Brief — 2026-08-05

Project: UE 5.8 JRPG / QuillScript "persona-lite" gameplay loop. Repo root: `BS_GodFile` (`BS_GodFile.uproject`).

## Live verification

| Check | Result |
| --- | --- |
| Editor process | PID 50152 up |
| Monolith MCP `:9316` | reachable |
| git push to origin | blocked (`github.com:443` unreachable) |
| git repo | healthy at `C:\EnvironmentPortfolio\BS_GodFile` — a recovered-`main` clone, NOT under the old corrupt `.git` |

## Recent changes & working tree

Commit tail:

| Commit | Description |
| --- | --- |
| `6154cc1e` | large resync |
| `62eab10d` | 48h T3D review |
| `d6ea9a1d` | ROADBLOCKS resolution |
| last few | feature + deploy commits |

Uncommitted / working tree (tagged **persona-lite** vs **exploratory**):

| File | Change | Tag |
| --- | --- | --- |
| `MelodiaTraversalComponent` | swim/dive (+434/+89, contains dead code) | persona-lite |
| `MelodiaSmokeCharacter.h` | skirt-cloth bool | persona-lite |
| 3 Monolith enum-pin fixes | `PC_Enum` → `PC_Byte` | persona-lite |
| Water plugin + Dive input | swim/dive wiring | persona-lite |
| GameInstance uasset + map binary edits | binary asset changes | persona-lite |
| `CURRENT_STATE.md` | state doc | persona-lite |
| `target_file.md` | deleted | exploratory |
| `wire_enemy_battles.py` | untracked | exploratory |
| `_fix_enum_pin_type.py` | untracked | exploratory |
| `_probe_pin_api.py` | untracked | exploratory |

## Persona-lite gameplay loop wiring state

Loop: dialogue → allowlisted encounter request → JRPG battle with Melusina → typed result → Quill resumes exactly once → exploration.

- Opening half verified in PIE.
- Battle-and-return half C++-wired but **ZERO PIE proof**.

CRITICAL structural finding: the battle-start request `OnBattleRequested` is bound by BOTH `UMelodiaExternalBridgeSubsystem` AND `UMelodiaBattleAdapterSubsystem` — a **duplicate whole-start hazard**.

All 14 persona P0 gates remain runtime-unproven:

1. terminal-result matrix
2. save/load process-restart round-trip
3. flag + reward restore
4. Quill-unavailable load
5. missing-script → safe site
6. manual-save-during-battle
7. Main Menu Continue/Load wiring

Continue/Load intentionally disabled. Rhythm/Harmonix/UI-skinning optional — not blockers.

## The unit that runs in need of wiring

- `OnBattleCompleted` delegate + `HandleBattleCompleted` binding on `BP_MelodiaSirMelodyMorningIntro`.
- Enum-pin fix (`PC_Byte`) currently baked via Monolith 3 files, but not yet live-coded into the running editor.
- Leftover BP delegate specs (`onbattlecompleted_spec.json`, `bind_onbattlecompleted.ps1`) vs the C++-side recommended path.

## T3D workspaces & tooling state

T3D = Unreal asset-text workspace.

- 23 exports; 5/23 (21.7%) migrated — all "Exploration".
- 18 combat/HUD/dialogue/menu stock awaiting bulk re-inject.
- Exec-pin wiring stays manual: `build_blueprint_from_spec` wires injected-subgraph internal exec only; a second `connect_pins` pass is needed for stock-node `then`/`execute`.
- Broken tools flagged: `t3d_demo.py` wrong tool-name; T3D injector CLI arg-mismatch + undefined methods; `fix_onbattlecompleted.py` invalid APIs (read-only diagnostic at `Saved/T3D/fix_onbattlecompleted.py` — never mutates assets).
- Hardcoded OpenRouter key in `nl_to_blueprint.py` (credential hazard).
- Stale `_export_results.json` (6 widgets at tiny bytes still logged while re-exported).

## Scaffolding / MCP / docs

Canonical doc hierarchy: decision log through `042`. `_TASK_QUEUE.md` granular P0–P3. `_VERTICAL_SLICE_SCOPE` as scope authority. `_ROADBLOCKS` as register. Authority & no-parallel rule enforced.

MCP bridges:

| Bridge | State |
| --- | --- |
| monolith | live `:9316` |
| it-is-unreal | `:8088` |
| ueblueprintmcp | disabled by default |
| ollama | present |
| deepseek-v4 | present |
| kimi-k3 | present |
| cpp-compile-feedback | present |

Stale pointers: `DOC_INDEX` counts "011"; `CURRENT_STATE` dated 07-02; some `.opencode.json`; `G:\` paths.

Tension: Decision 025 (don't run both Unreal bridges same-graph) vs QUelE rule #9 (run together).

## Next actions (highest leverage)

1. Bake Monolith pin fix into the running editor, then run one persona terminal path.
2. Single PIE walk driving `melodia:battle` to prove exactly-one battle-start / typed `CompleteBattle` / `resumeQuill`.
3. Fix the T3D tool bugs and gate the stale `.pyc`-reconstructed scripts (do not run 4/5).
4. Hygiene: move OpenRouter key out of source; fix `DOC_INDEX` + `.opencode` `G:\`; decide the my-site-clean nested repo.

---

Informational brief only. Governance remains authoritative via the project's own working/decision docs (`_TASK_QUEUE`, `_ROADBLOCKS`, decision log, `_VERTICAL_SLICE_SCOPE`). Appended 2026-08-05.
