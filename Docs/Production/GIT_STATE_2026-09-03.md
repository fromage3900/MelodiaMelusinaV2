# Git State & Push Queue — 2026-09-03
> **For:** Future Hermes sessions + laptop coordination
> **Network:** ❌ Blocked (port 443 + 22 both timeout). Local commits ready.

---

## Local Commits Awaiting Push

| Commit | Message | Files | Size |
|--------|---------|-------|------|
| `a9ab5433` | `docs(session): add 2026-09-03 session summary` | 2 | ~12KB |
| `d6cd9e03` | `docs(university): add entry-point docs, portfolio, index, animation plan, GN reference, infrastructure plan` | 41 | ~9.3KB insertions |

**Total ahead of origin/main:** 2 commits

---

## When Network Restores

```bash
cd C:\EnvironmentPortfolio\BS_GodFile
git push origin main
```

If HTTPS still fails, try SSH:
```bash
git remote set-url origin git@github.com:fromage3900/MelodiaMelusinaV2.git
git push origin main
```

---

## Laptop Coordination

### Clone (when needed)
```bash
git clone --no-checkout https://github.com/fromage3900/MelodiaMelusinaV2.git
cd BS_GodFile
git sparse-checkout init --cone
git sparse-checkout set Source Tools Docs Content/Melodia Content/MelodiaIntegration
git checkout main
```

### After laptop clone, to get full archive later
```bash
git sparse-checkout disable
git lfs fetch --all
```

---

## Triage: What's Tracked vs Not

### ✅ Tracked (in git)
- `Source/` — all C++ code
- `Tools/` — all Python tools, GN addons
- `Docs/` — all docs (including new ones from today)
- `Content/Melodia/` — ship-ready game content
- `Content/MelodiaIntegration/` — configs, allowlists
- `Content/Characters/` — Melusina, NPCs
- `Content/EnvSandbox/` — environments
- `Content/TurnBasedJRPGTemplate/` — stock template (selective)
- `Plugins/` — selective (QuillScript, Monolith, etc.)
- `deploy/`, `specs/`, `projects/`

### ❌ Not Tracked (local only)
- `Content/_ThirdParty/` — re-downloadable
- `Binaries/`, `DerivedDataCache/`, `Intermediate/` — generated
- `Saved/` — generated (except `Saved/Audit/`, `Saved/Portfolio/`)
- `CompatibilityLabs/` — backups
- `Imports/` — except `Imports/Animations/Cascadeur/`

---

## Key Session Findings (for future Hermes)

### Geometry Nodes
- 10 systems audited, 60+ builders mapped
- Full reference: `Docs/Research/GEOMETRY_NODES_COMPLETE_REFERENCE.md`
- Primary authority: `deploy/surreal_arch/melodia_gn/`

### Animation Pipeline
- 12 systems audited (mocap, FACS, retarget)
- 30+ mocap clips already retargeted
- Plan: `Docs/Production/CHARACTER_ANIMATION_2_SEMESTER_PLAN.md`

### Infrastructure
- LFS: 13667/13675 objects pushed (8 missing nebulae, harmless)
- .gitignore bug fixed: `research/` → `/research/`
- Laptop strategy: sparse checkout, tiered repo

### Network
- Port 443 (HTTPS) and 22 (SSH) both timeout
- Ping works, but git push fails
- Workaround: VPN, mobile hotspot, or wait

---

*Updated: 2026-09-03 | Local HEAD: a9ab5433 | Push: PENDING*
