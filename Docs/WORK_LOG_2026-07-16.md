# Work Log — 2026-07-16

Session summary of version-controlling the day''s uncommitted work on branch
`feature/touchdesigner-mcp-integration`. This captures combined work by the
user, Claude, and Codex, brought under Git (with Git LFS for binaries) in
logical, bucket-scoped commits. No history was rewritten and nothing was pushed.

## Commits (chronological)

### Agent config & repo governance
- `b0314da` — chore: agent config, Cursor rules, LFS attributes, repo governance
- `b3b674f` — chore: extend Git LFS coverage to `.toe`/`.tox`/`.blend`/`.vrm`/`.usd` + web/audio

### Melodia Python & tooling
- `d081288` — feat: Melodia Python game/tooling code + surreal-arch generators

### Docs & audits
- `7f165c2` — docs: Melodia production docs, material/PCG audits, audit notes
- `eff468f` — docs: Melodia project docs, pipelines, research, agent governance

### Unreal
- `be42580` — feat: Unreal game Content — assets, maps, characters, materials (LFS)
- `8f41726` — feat: Unreal project scaffold — uproject, C++ Source, Plugins, Config

### TouchDesigner
- `9c42e6c` — feat: TouchDesigner grandmaster_melodia — `.toe`/`.tox` (LFS) + scripts

### Web
- `b30e3aa` — feat: portfolio site clean image assets (LFS)

### Assets (Blender / FBX / Melusina)
- `a34781c` — feat: Blender assets, FBX products and exports (LFS)
- `bc82565` — feat: Melusina wardrobe textures + portfolio stage blends (LFS)

### R&D (voice synth, orrery, roof generator)
- `f88d711` — feat: root R&D scripts + orrery/material-maker design docs
- `2486c5a` — feat: voice-synth R&D plans + JD2 corpus (LFS)
- `62fbc65` — feat: Fromages Roof Generator Blender addon (v3.1) + review notes

## Git LFS

All binary buckets were pointerized (verified `version https://git-lfs.github.com/spec/v1`).
LFS coverage was extended before committing binaries:
- root `.gitattributes`: `.toe`, `.tox` (from plain binary), plus `.blend`/`.png`/`.jpg`/`.wav`
- `BS_GodFile/.gitattributes`: `.blend`, `.vrm`, `.usd` (in addition to existing UE patterns)

## Deliberately left uncommitted

- `BS_GodFile/Backups/Melodia_Characters_Melusina_*/**` — timestamped backup dump
- `._`-prefixed scratch: `BS_GodFile/._site_aside_untracked`, `BS_GodFile/._ghpages_work`
- Git worktrees / revert / archive: `BS_GodFile.worktrees/`, `BS_GodFile_revert/`, `_archive/`, `_staging/`
- Agent-loop machinery + stop-flags under `BS_GodFile/deploy/` (per no-stomp rule)
- Giant vendored/generated trees: `BS_GodFile/KitbashExport` (~28 GB),
  `BS_GodFile/Imports` (~1.2 GB), `_AssetLibrary/` (~10 GB), and
  `VoiceSynthResearch/{tools,release_candidate,data,prototype,...}` (~27 GB)
- Nested deploy mirrors: `BS_GodFile/_github_deploy`, `BS_GodFile/my-site-clean-ghpages`
- Vendored plugin git store: `BS_GodFile/Plugins/**/.git_disabled`
- Agent scratch: `BS_GodFile/.codex_staged_paths.txt`, `BS_GodFile_untracked_20260712.txt`