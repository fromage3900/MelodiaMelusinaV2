# PIE — 2026-08-11 (owner, MelodiaMelusinaV2)

**Source:** owner playtest notes via [Grok share](https://grok.com/share/bGVnYWN5LWNvcHk_c7761e0a-252b-44bf-b4de-4940025d6de0).  
**Repo:** public V2. **Runtime truth** — not static graph proof.

## Findings

| # | Finding | Class | Blocks |
|---|---------|-------|--------|
| 1 | Rhythm highway has **no notes** → nothing to hit on beat | Chart / session start / data channel | Rhythm juice (P1); not stock JRPG authority |
| 2 | **Sir Melodious** not on **CTRL** party switch | Party / companion registration | Co-op / explore switch |
| 3 | **UI backgrounds still transparent** | UMG style / alpha | Battle/rhythm HUD readability |
| 4 | On **L_KaleidoNave**: traversal, Quill NPC trigger, battle trigger **exist but do not fire** | Wiring / GM / interact / allowlist | **Whole First Dream loop** |

**Headline:** Level has actors/nodes, not a working loop. Same failure family as 2026-08-06 owner verify (dialogue/battle non-functional), now narrowed to KaleidoNave + specific systems.

## Gate impact

Keep **all** Echo foundation runtime gates **OPEN** (`runtime`, `save_load`, `repeat_consume`, `package_launch`). Do not `record_gate.py … pass` from “actor exists.”

Rhythm empty highway is **P1 juice** after stock Attack works — unless a given build requires rhythm for damage (A/B with `melodia.Rhythm.Disable`).

## Debug order (one at a time)

1. **RT-001** — One Quill NPC: text **visible**, focus, advance, terminal once  
2. **RT-002** — One battle trigger: stock UI visible + Attack resolves  
3. **RT-003** — Opaque panel backgrounds (not text-only alpha)  
4. Then Sir CTRL (**RT-005**), rhythm notes (**RT-006**), save restart (**RT-007**)

### Kaleido “exists but doesn’t work” checklist

1. Map GameMode / GI = `BP_MelodiaJRPGGameMode` / `BP_MelodiaJRPGGameInstance`
2. PC owns interaction + Enhanced Input contexts
3. Quill: collision/interact, script soft refs, widget opacity > 0
4. Battle: tag `melodia_smoke_encounter` (no `Encounter_` prefix); live `StartBattle` / `OnBattleOver`
5. Travel: `UMelodiaTravelSubsystem` allowlist; destination spawn tag present

Prefer one PIE + log filter (`MELUSINA_`, Quill, Travel, Battle) over more graph screenshots.

## Consequence

First Dream loop remains unproven. Grief hook `.qsc` stays authored, not play-proven, until Quill shows.

See: [`Docs/PhoneOps/HIGHEST_LEVERAGE_NOW.md`](../PhoneOps/HIGHEST_LEVERAGE_NOW.md).
