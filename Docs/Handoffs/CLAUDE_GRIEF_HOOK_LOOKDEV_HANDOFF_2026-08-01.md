# Claude handoff — grief-hook presentation + rebuild window

## Current live truth

- `M_PP_StorybookOutline` is already live through `PPV_NikkiDream` on `L_KaleidoNave`, `L_MelusinaMorning`, `ZenForestTest`, and `L_FallenMoon`.
- The corrected master includes buffer-UV taps, buffer-rect clamping, and normalized/max-based normal-edge handling. Do not replace it with a Codex candidate.
- `M_PP_StorybookOutline_LookdevCandidate` is only a parameter-tuning duplicate of that corrected master. It is **not** an eight-direction algorithm upgrade.
- Codex's Melu grade, Starry Night, sky MPC, and Niagara assets are unassigned candidates. No map/PPV/UDS attachment was made by Codex.

## Grief-hook presentation direction

The locked hook is: a small benign absence is experienced as catastrophic in the dream; Sir remains alive and retrievable; the older duet-partner remains an absence-with-shape; final register is warm reunion. It must land through existing narrative/pacing/palette systems only.

- **Morning:** warm, quiet, materially grounded; held ambience, no global distortion.
- **Dreamstate:** saturated/strained but readable; surrealism expresses interpretation, not a separate horror mode.
- **Zen Forest:** sparse "place that listens" accents; no VFX or PPV effect may block route/UI, gate battle, or become a gameplay state.

`MelodiaMorningIntro` now has one additive first line: “The perch is empty again. I know a small absence can fill a whole room.” It compiles, and all original labels, choices, `melodia:battle:melodia_smoke_encounter`, terminal branches, reward/flag commands, and `$ End` remain intact.

## Rebuild and A/B gates

1. Preserve the live outline master during the current rebuild.
2. Persist Custom Depth-Stencil as **Enabled with Stencil** before Kiro's visual stencil work is tested; until then the style branches are invisible.
3. The remaining PPV question is the unresolved ~0.70 screen-rectangle artifact. Run the decisive PostProcessing-off / PPV-disabled viewport check before shader changes; it may be capture/TSR rather than material math.
4. Test SDF fish and petal translucency specifically against the live PPV. Translucent/additive particles are intentionally outside the depth/normal outline; preserve their alpha/emissive read.
5. If temporary A/B is needed, attach a matching candidate pair only to a temporary PPV. Keep `PPV_NikkiDream` on the approved master until the owner approves a visual promotion.

## Ownership boundary

Claude owns PPV attachment, `MPC_PPBlending`, and outline material maintenance. UDS remains weather/sky/light authority. Kiro may set/clear component stencil values as one-way presentation only. Codex owns unassigned Niagara/material candidate work and will not write PPV, UDS, maps, or `MPC_PPBlending`.
