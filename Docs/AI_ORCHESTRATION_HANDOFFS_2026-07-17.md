# AI Orchestration — Vertical Slice War Room (2026-07-17)

Coordinator: Claude Fable (this chat) — design authority, sequencing, merge arbitration, editor-access scheduling.

## STATE UPDATE 5 (2026-07-19 — Sonnet overnight HUD/menu pass — READ THIS FIRST)

**Landed** (commits `40a2d391`, `eae77ec0`, `c0fe3cf5`, `7bc39788` on `codex/integration-gameplay-loop-20260718` — note: this session's work landed on this branch, not `feature/touchdesigner-mcp-integration`; reconcile before assuming a single lineage):

- **6 new WBP menu panels authored**, all under `/Game/Melodia/UI/`: `WBP_MenuButton` (reusable atom), `WBP_ComicOrrery` (docker/pie-menu, doubles as main menu per user decision, 4 sphere buttons live-wired to `OpenLevel` via a new `DA_OrreryRegistry` data asset), `WBP_BlessingBurden` (dungeon reward menu, 3 cards live-wired to `UMelodiaRoguelikeRunSubsystem::CommitReward`), `WBP_DialogueBubble`/`WBP_SkillCodex`/`WBP_QuestJournal` (visual shells, data binding still pending), `WBP_Settings` (all 4 motion-tier buttons live-wired to `UMelodiaSaveGameSubsystem::SetMotionTier`). All share Persona-style tilted crimson accent slashes on top of the existing SoftMG/Baroque chrome.
- **HUD texture reskin**: `MelodiaRhythmHUDWidget::LoadPresentationTextures()` now pulls the current SoftMG-named textures instead of the deprecated Batch N naming (HighwayBG/Hitline/NoteHead/NoteHeadBeam) — matches `my-site-clean/generated/assets/melodia-game-ui/ART_SOURCE.json`'s `deprecated_batch_n.replacement` map. 17 textures imported total this session (7 SoftMG + 7 portfolio-chrome/Baroque + 3 more Baroque atoms that were staged but never actually imported under their real names).
- **New small C++**: `AMelodiaQuestManagerBase::Get()` locator (wiring-plan blocker C3a), `MotionTier` field on `UMelodiaSaveGame` + `Get/SetMotionTier` on `UMelodiaSaveGameSubsystem` captured/restored on save/load (blocker C4), `UMelodiaOrreryRegistry` data asset class (blocker C6).
- **Real gameplay-integration fix, not just asset authoring**: `BP_MelodiaDungeonRunCoordinator::PresentRewardChoices` previously only showed a status-text banner and never actually spawned a reward UI — now creates + adds `WBP_BlessingBurden` to viewport. This is the difference between "menu exists" and "menu actually appears when the game calls for it."
- **`ToggleOrreryMenu()`** added to `BP_Melusina` (BlueprintCallable) — creates/removes `WBP_ComicOrrery`, tracked via new `ActiveOrreryWidget` var. PIE-verified via console-injected function call, zero runtime errors. **Not yet bound to an actual keypress** — `IA_ToggleOrrery` + `IMC_MelodiaDefault` (Enhanced Input assets) exist but hit a genuine tooling wall: `K2Node_EnhancedInputAction`'s `InputAction` reference isn't configurable via any available Monolith MCP action, and direct Python reflection also fails (`EdGraph.Nodes` is protected in UE's Python API). **Someone needs to open BP_Melusina in the editor UI and manually add the Enhanced Input event node + bind it to `ToggleOrreryMenu` — ~30 second manual task**, or find/build proper MCP tooling for it.

**Two real incidents found and fixed, not cosmetic:**
1. **`BP_Melusina` was corrupted** (truncated to 643 bytes on disk, unreadable by the editor) — almost certainly from an earlier full-disk incident. Fixed by locating the correct 40302-byte object in the local `.git/lfs/objects/` cache and copying it directly over the truncated file (a straight `git checkout` failed with "unable to unlink," meaning the editor had a lock on it — had to close the editor first). Verified byte-for-byte via SHA256 against the LFS oid. **Note: the presentation-interface hookup done earlier in this same overnight session (OnMelodiaVictory/Defeat/CommandResolved via `add_function_override`) was never committed before the corruption and is now lost** — needs redoing if wanted. The reactivity BeginPlay wiring (`RegisterReactiveMeshComponent`) survived since it was committed.
2. **`BP_MelodiaGameMode.DefaultPawnClass` regressed to `None`** on disk (uncommitted) — found the morning after a PC/editor restart, this is why the user saw no active pawn and no HUD. Restored to `BP_Melusina_C`, PIE-verified ("HUD widget spawned" fires exactly once, zero errors), committed (`7bc39788`).

**Given two silent regressions in one session (a corrupted asset, a nulled CDO reference), it's worth being suspicious of anything else that looks "off" after this point rather than assuming it's user error** — spot-check `get_cdo_properties`/`get_blueprint_info` against expectations before building on top of something that looks broken.

**Confirmed still-blocked, pre-existing (not caused this session)**: `AMelodiaDungeonRunCoordinator` has never actually been placed in any level — it exists only as a class, confirmed via `find_references` returning zero results. Spawning one at runtime and calling `StartFirstDungeonRun` on it silently early-exits (no `DungeonGenerator`/room actors assigned — those only exist wired up in a properly-authored dungeon level). This is downstream of the ground-validation bug flagged in STATE UPDATE 3 (`MelodiaRoomEntrance_0`, "No safe ground found beneath entrance") — still unresolved, still the real blocker on any live dungeon-loop testing.

**Next priorities, in order:**
1. Manually bind the Enhanced Input keypress for `ToggleOrreryMenu` in the editor UI (30 seconds, see above).
2. Live data bindings for `WBP_DialogueBubble` (spawn from `UMelodiaNPCInteractionComponent::OnInteractionStarted`/`OnDialogueLine`, same pattern as the BlessingBurden fix), `WBP_SkillCodex` (populate from `BuildDemoSongSkills()`), `WBP_QuestJournal` (populate from `AMelodiaQuestManagerBase::Get()`).
3. Fix the `MelodiaRoomEntrance_0` ground-validation bug so a real dungeon run (and therefore a real, in-context test of `WBP_BlessingBurden`) is actually reachable.
4. Redo the lost presentation-interface hookup on `BP_Melusina` (victory/defeat/hit reaction animations) if still wanted — proposal doc with real asset names already exists: `Docs/PRESENTATION_INTERFACE_INTEGRATION_PROPOSAL_2026-07-18.md`.
5. `WBP_Sparkle` shared burst/drift sub-widget so the Persona accent slashes actually pulse on interaction instead of sitting static.
6. Reconcile branch lineage — confirm whether `codex/integration-gameplay-loop-20260718` (where tonight's work landed) and `feature/touchdesigner-mcp-integration` (referenced in earlier STATE UPDATEs) need merging, or whether the canonical worktree has simply moved.

---

## LANE REASSIGNMENT (2026-07-18, same day as STATE UPDATE 4 — user-directed)

**DeepSeek moves off C++ Finisher duty and onto the TouchDesigner integration lane** (was HANDOFF 3, "Dungeon C++ Integration" — that lane's remaining C++ items fold into HANDOFF 6/GPT below, since GPT already holds "C++ Finisher"). **Codex/GPT gets a new goal: the data-driven NPC scaffold** (world-building unblock — see rationale below). New/updated handoffs:

### HANDOFF 3 (REWRITTEN) — DeepSeek — "TouchDesigner Integration Lane" (no UE editor access; TD + disk only)

PASTE:

> You are the TouchDesigner Integration agent for the Melodia project. Two surfaces: `C:\EnvironmentPortfolio\_TouchDesigner\grandmaster_melodia\` (the .toe project, driven via the `envoy` MCP — `mcp__envoy__*` tools: get_td_status/launch_td/create_op/set_parameter/connect_ops/execute_python/etc.) and `C:\EnvironmentPortfolio\BS_GodFile\Plugins\MelodiaCore\Source\MelodiaCore\` (UE-side C++ — you may author/compile but do NOT drive the Unreal editor; coordinate any cold-build window with the user, same rule as before: `Build.bat BS_GodFileEditor Win64 Development -Project=C:\EnvironmentPortfolio\BS_GodFile\BS_GodFile.uproject -NoUBA -MaxParallelActions=4`, editor must be closed).
> HARD RULES (unchanged from every other lane): finish existing documented systems, never invent parallel ones; scoped commits with exact paths; never delete assets; coordinate editor/build windows with the user.
> READ FIRST: `Docs\UNIVERSAL_MUSICAL_INFLUENCE_SCAFFOLD_2026-07-18.md` (the UE-side signal source and what's already broadcasting) and `_TouchDesigner\grandmaster_melodia\scripts\wire_battle_osc.py` (the existing UE→TD channel — an OSC In CHOP on **port 9000**, already receiving 14 discrete battle-event routes from a UE-side `battle_osc.py` sender).
> TASK LEDGER, in order:
> 1. **Reconcile the port discrepancy first, before building anything**: HANDOFF 6/GPT's brief (below) separately cites port 9870 for OSC battle events, but the actual configured `wire_battle_osc.py` CHOP listens on port 9000. Find and read whatever UE-side `battle_osc.py` sender currently exists (search `Content/Python/` and the plugin source) to see which port it actually sends to, and make port 9000 the single source of truth end to end — don't stand up a second channel on 9870.
> 2. **Stream continuous beat data, not just discrete events**: `wire_battle_osc.py` currently only receives discrete battle triggers (hit/break/victory-style). Add new OSC routes (reuse the port-9000 channel) for `BeatPulse` and `BeatPhase` published every tick by `UMelodiaRhythmReactivitySubsystem::Publish()` — these are the two continuous signals TD-side geometry/shader work needs to breathe with the music. On the UE side this likely means extending whatever `battle_osc.py`-equivalent sender exists to also emit these two floats per publish (ask the coordinator if no sender exists yet — don't invent a second reactivity system, this one is the sole source of truth per the scaffold doc).
> 3. **TD-side consumption**: once BeatPulse/BeatPhase land on the OSC In CHOP, wire at least one visible TD network response to them (a simple pulse-driven parameter on an existing op in `grandmaster_melodia.toe` — check `scripts/wire_fractal.py`/`wire_render.py` for existing patterns to extend rather than new ops) as a proof point, matching the "subtle, not overdone" direction already given for the UE-side material reactivity.
> 4. **Report back**: which port is authoritative, what routes exist now, a screenshot/capture of the TD-side response, and the UE-side diff + build log if you touched C++.

### HANDOFF 6 ADDITION — Codex/GPT (C++ Finisher lane) — NEW GOAL: Data-Driven NPC Scaffold

PASTE (append to existing HANDOFF 6 brief, or paste standalone if starting fresh):

> New task, added ahead of the OSC item (HANDOFF 6 item 3) since DeepSeek now owns that — see LANE REASSIGNMENT note. Rationale: the user has a lot of world-building/layout/NPCs left to make, and hand-wiring one Blueprint per NPC doesn't scale. This project already solves exactly this problem for enemies (`FMelodiaEnemyDef` + JSON drafts in `Imports\Data\` feeding definition assets) and rooms (`RoomData` + the GMM manifest pipeline) — mirror that pattern for NPCs instead of inventing a new one.
> TASK LEDGER:
> 1. **Define `FMelodiaNPCDef`** (USTRUCT, mirror `FMelodiaEnemyDef`'s shape/conventions) covering whatever a generic NPC needs to configure itself from data: display name, mesh/skeletal-mesh soft references, default dialogue key(s) (align with the VN dialogue drafts already accumulating in `Imports\Data\Dialogue` per HANDOFF 4), an AI-behavior tag or behavior-tree soft reference, spawn-transform-relevant metadata if useful (e.g. a "role" enum: Merchant/Quest/Ambient/Companion). Keep it additive/minimal — don't design features that don't have a consumer yet.
> 2. **One generic reference Blueprint** (new — this is the one BP a human/editor-lane agent authors by hand, not you, since you don't drive the editor; write the C++ base class it derives from, e.g. `AMelodiaNPCBase`, with a `TSoftObjectPtr<UMelodiaNPCDefinition>`-style data-asset reference and BeginPlay logic that reads the row and configures mesh/AI/dialogue — mirror however `UMelodiaRunArchetypeDefinition` consumption was wired for the dungeon coordinator, same fallback-when-unset pattern).
> 3. **DataTable/DataAsset import path**: propose (one md, don't build the importer yet — same caution as HANDOFF 3's old M3 item) a `Imports/Data/NPCs/*.json → UMelodiaNPCDataAsset` editor-python importer respecting `Imports/Data/VALIDATION.md` verdicts, so Ollama's existing content-grind loop (HANDOFF 4) can start drafting NPC rows the same way it already drafts enemies.
> 4. **Batch-population hook**: expose one `BlueprintCallable` static or subsystem function that takes an array of `(FMelodiaNPCDef row ID, FTransform)` and spawns/configures NPCs from it in one call — this is what lets the editor lane (or a future Python/Monolith `batch_spawn_blueprint_actors` call) populate a level's worth of NPCs from an authored layout list instead of placing them one at a time.
> DELIVERABLE: diff, `Result: Succeeded` build log, 5-line summary of what the editor lane needs to do next (author the one reference BP, then start populating).

---

## STATE UPDATE 4 (2026-07-18 — Sonnet solo pass, current authority — READ THIS FIRST)

**Landed this pass (commits `188f6dc8`, `498aab64` on `feature/touchdesigner-mcp-integration`; editor left running, Monolith bridge healthy):**
1. Fixed a real bug: `UMelodiaRhythmReactivitySubsystem::Publish()` was writing 12 named scalars to `/Game/EnvSandbox/Materials/Functions/MPC_Portfolio_Audio`, which only exposes 2 of those names — every other write was a silent no-op, so gameplay had **zero material reactivity** despite the Quartz beat clock running correctly. Repointed to `MPC_Melodia_Palette` (17 matching params), remapped creatively (combo shifts palette, victory blooms sparkle, tension jitters time).
2. Caught and reverted my own mistake mid-session: initially wired beat-reactivity directly into `M_MooaToon_Glitter_Iridescent`'s graph — **user correctly flagged this as a deprecated material family** ("old materials, only use new MF functions or universal master materials"). Confirmed it isn't even referenced by Melusina's live mesh, fully reverted the graph edit, and rebuilt the approach around `M_Master_Toon_Universal`'s own exposed knobs (DreamPulseAmp/Iridescence/TemporalStrength/ParallaxStrength — 291 params, all real, most currently at inert defaults) via **per-actor dynamic material instances**, not a shared-graph edit. Rule for every lane: **never edit `M_MooaToon_*` — that family is deprecated. Only `M_Master_Toon_Universal` / current MF_ functions.**
3. Added `UMelodiaRhythmReactivitySubsystem::RegisterReactiveMeshComponent(UPrimitiveComponent*)` — generic, not Melusina-specific; creates MIDs on every material slot of whatever mesh you pass it, and `Get(WorldContextObject)` static accessor for reliable BP access. Cold-build verified (`Result: Succeeded`).
4. Wired `BP_Melusina`'s BeginPlay to call `RegisterReactiveMeshComponent(GetMesh())` — she is now live-reactive to the beat. Compiled clean, 0 errors/warnings.
5. Two presentation-interface silent no-ops found and fixed: `OnMelodiaDefeat()` added to `IMelodiaCombatPresentationInterface` + call site in `MelodiaBattleSession`'s Defeat-phase transition (was missing entirely — victory had a hook, defeat didn't); `SubmitUltimateCommand()` had zero HUD calls (separately caught and fixed per STATE UPDATE 3 — confirmed still in place).
6. Wrote `Docs\UNIVERSAL_MUSICAL_INFLUENCE_SCAFFOLD_2026-07-18.md` — names and documents the project-wide "world reacts to music" mechanic per the user's direction (subtle, incremental, not overdone). Key finding: the mechanism to make **any** actor or environment material music-reactive already exists and needs no new C++ — either sample `MPC_Melodia_Palette` directly in a material graph (any material, zero code), or call `RegisterReactiveMeshComponent` in an actor's BeginPlay (any actor). Nothing else has opted in yet. Also confirmed `_TouchDesigner/grandmaster_melodia/scripts/wire_battle_osc.py` already has an OSC In CHOP on port 9000 for discrete battle events from a UE-side `battle_osc.py` sender — this is the channel to extend for continuous beat streaming (see HANDOFF 6 item 3 below, which separately cites port 9870 — **that port number needs reconciling with the actual `wire_battle_osc.py` port 9000 before anyone builds the emitter**, flagging not fixing).

**Confirmed still-open gaps (verified via asset search this pass, not just doc claims):** zero Blueprint anywhere in `Content/` implements `MelodiaCombatPresentationInterface` or `MelodiaEnemyPresentationInterface` (grepped every `.uasset`, zero hits) — the interfaces exist, call sites exist, but **nothing plays a mocap/VFX in response to them yet**. Same grep found **no docker/compendium/inventory/quest/main-menu WBP exists at all** — the "essential UI buttons" HUD panel the user asked about is a from-scratch build, not a wiring task.

**Revised next-tasks list to finalize game UI + HUD (in priority order):**
1. **Presentation interface implementers (highest-leverage, cheapest)** — on `BP_Melusina`: implement `OnMelodiaVictory`/`OnMelodiaDefeat` (play `AM_Mocap_Victory` / `A_Die`-based montage, both real assets per `Docs\PRESENTATION_INTERFACE_INTEGRATION_PROPOSAL_2026-07-18.md`), `OnMelodiaCommandResolved` (`AM_GetHit` on miss/hit reaction). On the enemy base BP: the four `OnMelodiaEnemyIntentStarted/Hit/Broken/Defeated` events. This is pure BP wiring against assets that already exist — no new C++, no new art.
2. **`WBP_Battle_Results` visual PIE confirmation** — authored and data-bound (STATE UPDATE 3) but never actually seen rendering in a live viewport (python probe couldn't resolve the PIE world handle). Add to viewport, trigger a battle, screenshot, confirm numbers populate.
3. **HUD docker panel (new build, not wiring)** — bottom-right panel: compendium/inventory/map/quests buttons, motion + sparkle/iridescence on click per the user's original ask. No backend systems exist for compendium/inventory/quests yet, so scope this as UI shell + `BlueprintImplementableEvent` stubs first (mirrors the `PresentRewardChoices`-style pattern already used elsewhere), not full feature implementation. Needs a Cursor spec first (HANDOFF 2 addition below) before Sonnet lane builds it — don't invent the layout blind.
4. **UniversalMusicalInfluence adoption pass** — pick 1-2 environment set pieces (not everything, stays subtle per user direction) to call `RegisterReactiveMeshComponent` in BeginPlay, as the first real "world reacts to music" proof point beyond Melusina. Candidates: something in `L_MelodiaGrove` or the bedroom's filigree trim, low published weights.
5. Ground-validation bug on `MelodiaRoomEntrance_0` (STATE UPDATE 3, still blocking every dungeon run past room 1) — unrelated to UI but still the highest-severity non-UI blocker; mention to whichever lane picks up level-art/generator work next.

**Handoff additions:**
- **HANDOFF 3 / DeepSeek (or whoever holds the C++ Finisher lane now — see HANDOFF 6, GPT)**: add a QUEUE 3 item — implement the presentation-interface BP hooks is Blueprint work, not C++, so this is actually a Sonnet-editor-lane task, not DeepSeek's; DeepSeek/GPT's C++ role here is none unless a new montage-play helper function is wanted (not currently needed — `PlayAnimMontage`/`Execute_OnMelodiaVictory` are both already BlueprintCallable/native-event, callable straight from BP).
- **HANDOFF 3b / Cline**: add to the disk-side validation pass — confirm `AM_Mocap_Victory`, `A_Die`, `AM_GetHit`, and the 3 `A_Mocap_LittleDance`/`A_Mocap_Twirl_001` assets referenced in `Docs\PRESENTATION_INTERFACE_INTEGRATION_PROPOSAL_2026-07-18.md` still resolve on disk (no renames/moves since that doc was written) before the Sonnet lane wires them — cheap, prevents a wasted editor session.
- **HANDOFF 2 / Cursor**: add WAVE 3 — a `Docs\Imports\UI\Specs\WBP_HUD_Docker.md` spec (Figma node-id, layout tree, 4 button states incl. hover/click motion + sparkle/iridescence treatment, bindings against whatever compendium/inventory/map/quest stub events the coordinator names) — needed before anyone builds the docker panel, per task 3 above. This is new scope, not in the original luxury-UI plan; Cursor should treat it as WAVE 3, not redo WAVE 1/2.
- **HANDOFF 6 / GPT (C++ Finisher)**: item 3 (OSC battle events → TouchDesigner) — reconcile port 9870 (as documented here) against port 9000 (as actually configured in `wire_battle_osc.py`'s OSC In CHOP) before building the emitter; also note `UMelodiaRhythmReactivitySubsystem` is now the correct single signal source to stream from (BeatPulse/BeatPhase in particular) per `Docs\UNIVERSAL_MUSICAL_INFLUENCE_SCAFFOLD_2026-07-18.md`.

---

## STATE UPDATE 3 (2026-07-18 overnight — Sonnet solo pass, current authority — READ THIS FIRST)

**Sonnet lane fully clean, 11 commits landed overnight on `feature/touchdesigner-mcp-integration`** (`847a637a` → `5d0cd216`). Editor left running, healthy, Monolith bridge up. All three items in STATE UPDATE 2's "open trio" are now closed:

1. **Quartz flip+beat-lock signed off** (`847a637a`): `bUseQuartzClock` now defaults `true`; `MelodiaBattleSession`'s encounter-start BGM routes through `StartBattleClock()`/`PlayBGMQuantized()` when the rhythm component opts in (auto-fallback to wall-clock otherwise). Cold-build verified.
2. **Generator BP interface — fully real now, not just a stub** (`847a637a` stub → `5d0cd216` real logic): `BP_RoguelikeDungeonGenerator` implements `IMelodiaDungeonRecipeConsumer`; `ChooseFirstRoomData` resolves the recipe's `RoomDataId` to a real `RoomData` asset via new `UMelodiaCoreRulesLibrary::ResolveRoomDataById()` (with literal fallback), `ContinueToAddRoom` now correctly returns `false` (one room per stage-recipe call — was hardcoded `true`, producing the same two literal rooms every run). PIE-verified: `Nb Room To Load: 1` (was 2), correct room resolved per stage archetype.
3. **Four-arrow PIE verify — partial, one real finding**: confirmed no `[MELODIA_EDITOR_ACTION]` error and the archetype-fallback log line firing correctly (`"Melodia: no archetype assigned for auto-start; using hardcoded stage pool."`). Did **not** exercise defeat-path/checkpoint-file/persistence-log arrows specifically — that needs an actual lethal-damage playthrough, not just a generation smoke test. Still open for whoever plays a full run.

**New, unrelated bug found (not fixed, flagged for level-art lane):** dungeon generation now correctly streams the resolved room, then rejects it — `"No safe ground found beneath entrance 'MelodiaRoomEntrance_0'"` → `"Melodia generation attempt 1 rejected after streaming: entrance=invalid exit=valid."` Room-geometry/entrance-socket issue on `L_MelodiaGrove`, reproducible every run right now. **This blocks any real playthrough past room 1 until fixed** — probably the actual next highest-leverage item.

**Also landed:**
- Glide ABP, Batch O textures + font, travel portal placement (all pre-existing from STATE UPDATE 2, confirmed still clean).
- `WBP_Battle_Results` authored + fully data-bound to `GetLastBattleResults()` (27-node EventGraph, compiles clean; **not independently visually confirmed in a live PIE viewport** — couldn't cleanly resolve the PIE world handle from a python probe, `unreal.WidgetBlueprintLibrary` isn't exposed in this build's Python API). `WBP_UltCutIn` authored as static layout (skill-name/portrait are real design gaps, not oversights — no source data exists for them yet).
- Found + fixed a second real gap: `SubmitUltimateCommand()` had zero HUD calls despite its own spec claiming sparkle/pulse were wired — added, matching the existing BREAK-event pattern.
- Confirmed the "6 art-less HUD texture slots" note in STATE UPDATE 2 was **stale** — all 6 already had real art committed (`be425804`) at the exact `LoadObject` paths. Nothing to do there.
- One housekeeping note: a stash entry (`stash@{0}`, superseded/obsolete WIP) sits in `git stash list` — a stash-drop got classifier-blocked mid-session, harmless, safe to drop whenever.

**Recommended next priorities, in order:**
1. Fix the `MelodiaRoomEntrance_0` ground-validation bug — currently hard-blocks every dungeon run past generation.
2. Full lethal-damage playthrough to actually verify DeepSeek's defeat/persistence arrows (not just generation).
3. Manual PIE spot-check of `WBP_Battle_Results` rendering (add to viewport, trigger a battle, confirm numbers show correctly) — the binding compiles clean but wasn't visually confirmed.
4. `ChooseNextRoomData`/`ChooseDoor` are still literal stubs — fine for now (unreachable with `ContinueToAddRoom=false`), but would need real wiring if multi-room-per-recipe generation is ever wanted.

---

## STATE UPDATE 2 (2026-07-17 ~16:30 — current authority; see also Revenue Architecture in the coordinator plan)
- **Sonnet wave LANDED, lane clean** (6 commits on `feature/touchdesigner-mcp-integration`): Glide ABP `8efae333`, Batch O textures + F_Melodia_UI font `50fa54a2`, Quartz battle clock C++ `a4073eff` (cold-build verified, `bUseQuartzClock` defaults FALSE — flip+verify pending), travel portal in L_MelusinaMorning `3bdbf0ab`. Editor exited clean.
- **DeepSeek's four dungeon arrows COMPILED CLEAN** (defeat path, persistence wiring, archetype consumption w/ hardcoded fallback, generator contract check w/ loud failure) — PIE verification pending, expectations written per-arrow in DeepSeek's summary. Its next queue is the monetization-persistence arrows (HANDOFF 3 below, updated).
- **Kimi**: busy on website renders — takes the editor token only AFTER Sonnet's UDS repair + arrow PIE-verifications.
- **UDS repair still pending** (script in the state block below) — sky stays broken until run; ignore UDS load errors.
- **Open trio gating Phase-1 sign-off**: Quartz flip+beat-lock PIE test · four-arrow PIE verify · generator BP interface implementation (design fix = Fable).
- **6 HUD texture slots still art-less**: highway BG, hitline, notehead, notehead-beam, enemy glow, skill chip → Cursor wave 2.
- **Daemon fleet EXPANDED** (all detached, `deploy/STOP_ALL` halts everything): slice-content (enemies/charts/mods), wardrobe-catalog (cosmetics → `Imports/Data/Cosmetics`), dialogue (`Imports/Data/Dialogue`), Gumroad/FAB listing copy (`Docs/Gumroad/Drafts`), and a deterministic JSON validator (`Imports/Data/VALIDATION.md`, quarantine LIST only). Launcher: `deploy/start_ollama_fleet.ps1`.
- **Monetization doctrine ratified (Nikki lens)**: cosmetics-only, never pay-to-win; gated via existing `ResolveCosmeticAvailability` content packs, all earnable with Heart/Swirl tokens; near-term revenue = Gumroad SKU #1/#1b (ship blockers: beauty pass, 6 screenshots, ZIP) then FAB "SDF Math-Art Toon Materials" (the 61 restored masters). In-game store waits until post-slice; only monetization C++ allowed now = persistence + entitlement provider (DeepSeek).

## STATE UPDATE (2026-07-17 ~15:00 — superseded by UPDATE 2 above)
- **Binaries are current**: cold build succeeded with staged enemy turns, arena camera phases + feel primitives (hitstop/shake/dilation/PerfectPop), Kimi's `FMelodiaBattleResultsSummary` (results-screen data now BlueprintReadOnly on BattleSession as `LastBattleResults`) and ult choreography. Editor relaunched on fresh DLLs.
- **Slice gap #1 CLOSED**: DefaultEngine.ini boots `L_MelusinaMorning` (committed `14c9b385`).
- **Cursor lane DELIVERED wave 1**: `Imports\UI\` has the Batch O filigree atlas (PNGs), three OFL fonts (TwinkleStar/Syne/InstrumentSerif), and 11 WBP build specs — Sonnet task #5 (editor import + brush binding) is now actionable.
- **Ollama daemon RUNNING** (`deploy/ollama_slice_content_daemon.py`, self-capping 48/24/20): enemy variants + charts accumulating in `Imports\Data\`.
- **Branch caution**: Kimi's commits live on local-only `feature/touchdesigner-mcp-integration` (LFS blocks push); working tree on `main` carries the same C++. Reconciliation is a COORDINATOR task — no lane rebases or force-pushes anything.
- **Lane change**: DeepSeek is now the Dungeon C++ Integration lane (HANDOFF 3, rewritten). Validation/hygiene moved to Cline (HANDOFF 3b).
- Kimi lane (HANDOFF 5) added; defeat-path/persistence C++ reassigned from coordinator to DeepSeek per user direction.
- **UDS "dead sky" diagnosed (~15:30)**: NOT data loss. Commit `c3fe101f` migrated UltraDynamicSky into `Content/_ThirdParty/` (813 adds, 0 deletes) but missed several Textures subfolders (Weather/ParticleClouds, Volumetric_Clouds/Cloud_Profile, Widgets/Clock_Face, …). Originals still exist under the old `Content/UltraDynamicSky/` root. FIX (coordinator, in progress): editor-side `duplicate_asset` of every old-root asset missing from the `_ThirdParty` twin (clean package names — do NOT file-copy uassets across package paths). Until it lands, UDS materials/aurora/clouds fail to load — ignore those load errors, don't "fix" them per-asset.
Target: **20-minute vertical slice** — wake in bedroom (save/bed) → ZenForest explore (jump/glide/Ctrl-swap flight) → 3-room roguelike dungeon (existing `BP_RoguelikeDungeonGenerator` + `MelodiaDungeonRunCoordinator` + rhythm battles) → boss → Victory results + rank.
Prime directive for every agent: **finish existing documented systems; never invent parallel ones.** Every task below cites its source-of-truth. If a task seems to need a new system, STOP and report back instead.

## Resource rules (hard)
- **Only ONE agent drives the Unreal editor at a time** (Monolith bridge, port 9316). That owner is the Sonnet chat below. Everyone else works disk-side or in external tools.
- C++ reflection changes (new UCLASS/USTRUCT/UENUM/UPROPERTY/UFUNCTION) ⇒ close editor → cold `Build.bat BS_GodFileEditor Win64 Development -Project=C:\EnvironmentPortfolio\BS_GodFile\BS_GodFile.uproject -NoUBA -MaxParallelActions=4` → relaunch. Live Coding is for function bodies ONLY (it crashed the editor twice today on reflection changes).
- Git: scoped commits only (list exact paths); never `git add -A`; never touch the two submodules or delete anything without explicit user go.

---

## HANDOFF 1 — New Claude chat (model: **claude-sonnet-5**) — "UE Editor Executor"
*(Sonnet 5 chosen: full MCP/tool competence at a fraction of Fable/Opus cost; Haiku is too weak to drive Monolith's 1300-action surface safely.)*

PASTE THIS AS THE FIRST MESSAGE:

> You are the UE Editor Executor for BS_GodFile (C:\EnvironmentPortfolio\BS_GodFile, UE 5.8). You have exclusive ownership of the Unreal editor via the Monolith MCP (editor_query/material_query/animation_query/blueprint_query…). Follow the approved plan at `C:\Users\froma\.claude\plans\let-s-start-with-the-quiet-petal.md` and the orchestration doc `Docs\AI_ORCHESTRATION_HANDOFFS_2026-07-17.md`. HARD RULES: (1) Finish existing documented systems, never invent parallel ones — cite the plan/doc section for everything you do. (2) Reflection C++ changes need editor-closed cold builds with `-NoUBA -MaxParallelActions=4`; Live Coding only for function bodies. (3) Scoped git commits with exact paths; never delete assets; create new ones only. (4) Gotchas: ISM `Transform(rotation=[pitch,yaw,roll])` (FRotator order); `render_preview` ignores MI overrides (use `get_instance_parameters`); non-sRGB textures need Linear samplers; never chain `load_level` over WP maps; PointLight non-inverse-squared intensity is single digits.
> Task queue, in order (UPDATED 2026-07-18 overnight — items 1-4 all DONE, see State Update 3 at top of doc. Next: fix the MelodiaRoomEntrance_0 ground-validation bug blocking every dungeon run, then a full lethal-damage playthrough):
> 1. Launch editor, wait for Monolith (port 9316). FIRST ACTION: run the UDS twin-repair python from State Update 1's fix note (duplicate_asset every `/Game/UltraDynamicSky/*` asset missing from `/Game/_ThirdParty/UltraDynamicSky/*`) — the sky is broken until this lands. Save + scoped commit.
> 2. Flip `bUseQuartzClock=true` (CDO or per-arena), PIE battle: verify taps on the metronome/BGM beat grade Perfect with `SW_BGM_Battle_Placeholder_128` playing beat-locked (128 BPM). This signs off Phase 1.4. If drift >1 beat, fall back to false and report readings.
> 3. PIE-verify DeepSeek's four arrows EXACTLY per its written expectations: lethal damage → defeat summary hook → travel to L_MelusinaMorning ("Melodia run: dungeon defeat" log); `Saved/SaveGames/MelodiaRunCheckpoint_v1.sav` appears per stage + clears at run end ("Melodia persistence:" logs); archetype field override vs hardcoded fallback logs; generator `[MELODIA_EDITOR_ACTION]` log if the interface is missing (if it fires, STOP dungeon testing and report — the interface fix is Fable's).
> 4. Author `WBP_Battle_Results` from `Imports\UI\Specs\WBP_Battle_Results.md`, bound to `UMelodiaBattleSession::GetLastBattleResults()` (struct shipped: Rank/Score/MaxCombo/Perfect/Hit/Miss/TotalNotes/DamageDealt) + `WBP_UltCutIn` per its spec, shown from the ult flow.
> 5. Import Cursor wave-2 HUD textures (6 missing slots) when they appear in `Imports\UI\`, bind on `MelodiaRhythmHUDWidget`.
> 6. After each milestone: save, scoped commit, PIE screenshot to the user.

---

## HANDOFF 2 — Cursor (Figma MCP) — "UI Asset & Spec Lane" (NO editor access)

PASTE INTO CURSOR:

> Execute the UE catch-up pass ("L3") of `Docs\MELODIA_LUXURY_UI_FILIGREE_NIKKI_MOTION_PLAN_2026-07-12.md` using `Docs\MELODIA_FIGMA_UI_WIRING_PLAN_2026-07-16.md` as the widget/node-id/binding map. Figma is SSOT — do not redesign anything. Deliver FILES ONLY into `C:\EnvironmentPortfolio\BS_GodFile\Imports\UI\` (never touch Content\*.uasset, never open Unreal):
> 1. `Imports\UI\BatchO\` — export the Batch O / Baroque filigree atlas (FiligreeCornerOrnate, DividerWave, CrestFinale, LaneRail, GradeHalo, CornerBaroque, DividerScroll, CrestBaroque, Medallion, Brace) as individual PNGs, power-of-two, transparent, 1024 or 512 per atom, plus the 4 grade-halo variants.
> 2. `Imports\UI\Fonts\` — Twinkle Star TTF/OTF if licensed, else Syne + Instrument Serif (per the luxury plan's type ramp).
> 3. `Imports\UI\Specs\` — one markdown spec per priority WBP (Battle_Command, Battle_Results, UltCutIn, MainMenu, SaveLoad, FieldHUD): Figma node-id, layout tree, colors/tokens, and the C++ binding each element reads (bindings are already enumerated in the wiring plan — copy them, don't invent).
> 4. Keep the RhythmReactivityBoard motion-channel matrix (Pillar C) in each spec so UE UMG animations can mirror the web motion.
> WAVE 2 (added ~16:30 — wave 1 received, thank you): (a) the 6 still-missing HUD texture slots at luxury-plan spec: HighwayBG, Hitline, NoteHead, NoteHeadBeam, EnemyGlow, SkillChipBG → `Imports\UI\Wave2\`; (b) final art for WBP_Battle_Results + WBP_UltCutIn (specs already delivered — now the actual layered art); (c) MONETIZATION (design only, no runtime implied): "Boutique" concept board — a wardrobe/cosmetic shop screen in the luxury filigree language, fed by `MELUSINA_BLENDER_WARDROBE_SSOT.md` vocabulary + the cosmetic drafts accumulating in `Imports\Data\Cosmetics\` (slots: dress/hat/gloves/shawl/trail/hair_charm; rarities Common→Grandmaster; Heart/Swirl token prices). Deliver as Figma board + one spec md.

---

## HANDOFF 3 — DeepSeek — "Dungeon C++ Integration Lane" (C++ authoring; NO editor driving)

PASTE:

> You are the Dungeon C++ Integration agent for BS_GodFile (C:\EnvironmentPortfolio\BS_GodFile, UE 5.8). All gameplay C++ lives in `Plugins\MelodiaCore\Source\MelodiaCore\`. Mission: correctly integrate the roguelike dungeon loop with MelodiaCore's authority classes and the GMM manifest contract (`Docs\MELODIA_GMM_FAMILY_ARCHITECTURE_PLAN.md` — GMM = the Unreal side of the Blender↔UE family; its contract is versioned JSON manifests validated by `Content/Python/validate_melodia_manifest.py`). Read `Docs\VERTICAL_SLICE_20MIN_REVIEW_2026-07-17.md` §"Ranked gaps" and `Docs\MELODIA_ROGUELIKE_GAMELOOP_COMPLETION_PLAN_2026-07-14.md` (RGL items) before writing anything.
> HARD RULES: (1) Finish existing documented systems — every change extends an existing class at an existing seam; if you believe a new class is required, STOP and write a one-paragraph justification to the coordinator instead. (2) You author and compile C++ but do NOT drive the Unreal editor; builds only when no UnrealEditor.exe is running: `"C:\Program Files\Epic Games\UE_5.8\Engine\Build\BatchFiles\Build.bat" BS_GodFileEditor Win64 Development -Project=C:\EnvironmentPortfolio\BS_GodFile\BS_GodFile.uproject -NoUBA -MaxParallelActions=4`. Coordinate the build window with the user. (3) Battle/turn math is FROZEN — touch wiring, not numbers. (4) Scoped commits on the current branch, exact path lists, no rebase/force-push, submodules untouched.
> THE FOUR BROKEN ARROWS (verified by C++ trace 2026-07-17 — fix in this order):
> 1. **Defeat path unwired (RGL-012)**: `UMelodiaRoguelikeRunSubsystem` has an EndRun/Defeated phase but `AMelodiaDungeonRunCoordinator` only handles `OnRunCompleted`. Add a defeat handler on the coordinator (bind the existing delegate surface; mirror `HandleRunCompleted`'s pattern): battle Defeat → run marked Defeated → defeat summary presentation hook (BlueprintImplementableEvent, matching the coordinator's existing Present* hook style) → return-to-bedroom travel (`L_MelusinaMorning`). No new classes.
> 2. **Persistence never invoked (RGL-012/014)**: `UMelodiaRoguelikePersistenceSubsystem` is complete but has ZERO callers. Wire: checkpoint save at each stage transition (`RequestNextStage`/`AdvanceStage` boundary), run-history append + checkpoint clear on run end (complete AND defeated), profile save via the existing `UMelodiaSaveGameSubsystem` at run end. Use its existing async API; bind its `OnOperationCompleted` for logging only.
> 3. **Definition data assets unread (RGL-009)**: the run subsystem hardcodes rewards/enemies; `UMelodiaRoguelikeDefinition` family (RunArchetype/RewardPool/Difficulty/Encounter) exists unread. Add soft-referenced archetype consumption: if a `UMelodiaRunArchetypeDefinition` is assigned on the coordinator, its pools feed stage recipes and reward candidates; when unset, the current hardcoded path runs untouched (same fallback pattern as `bUseQuartzClock`/`bStageEnemyTurns`). This is also the GMM hook: definition assets are what the manifest pipeline will populate — align field naming with the manifest vocabulary in the GMM plan §"project contract" and keep `validate_melodia_manifest.py` passing.
> 4. **Generator contract check**: `AMelodiaDungeonRunCoordinator` requires `BP_RoguelikeDungeonGenerator` to implement `IMelodiaDungeonRecipeConsumer` when `bRequireRecipeConsumer=true`. You can't open the BP — add a defensive log-and-fallback (clear UE_LOG error naming the missing interface + `RecordGenerationResult(false)` path) so a missing implementation fails loudly instead of hard-failing silently, and flag it for the editor lane to verify.
> DELIVERABLE per arrow: the diff, a build log showing `Result: Succeeded`, and a 5-line summary of what a PIE tester should observe. Enemy data from `Imports\Data\EnemyVariants\*.json` (ollama drafts, schema `FMelodiaEnemyDef-draft-v1`) may inform a follow-up importer script into definition assets — propose, don't build, until arrows 1-3 land.
> QUEUE 2 (added ~16:30 — the four dungeon arrows compiled clean; PIE verify is the Sonnet lane's): **monetization-persistence arrows**, all existing seams, math/battle untouched, same build-window rules:
> M1. Persist durable progression in `UMelodiaSaveGame` (additive v2 fields, migration-safe defaults): `Currency` (from `MelodiaProgressionComponent`), `HeartMelodyTokens`/`SwirlMelodyTokens` (from `UMelodiaRoguelikeRunSubsystem`), `OwnedContentPacks` (from `UMelodiaEntitlementSubsystem` — currently Transient). Wire capture in `SaveToSlot` and restore in `HandleAsyncLoadComplete` via the owning classes' own APIs (add narrow `Restore*` methods on them if none exist — mirror `RestorePersistentPartyState`'s pattern).
> M2. Implement a file-backed dev `IMelodiaEntitlementProvider` (JSON list in `Saved/DevEntitlements.json`) + call `RefreshCatalog` with it at startup — the platform-store implementer slot stays open and untouched. Cosmetic gating continues through `ResolveCosmeticAvailability` only.
> M3. PROPOSE (one md, don't build yet) the `Imports/Data/*.json → UMelodiaRunArchetypeDefinition / UMelodiaEnemyDataAsset / cosmetic definition` editor-python importer, respecting `Imports/Data/VALIDATION.md` verdicts (only passing files import).

## HANDOFF 3b — Cline — "Disk-Side Validation & Hygiene Lane" (NO editor, NO deletions)

PASTE:

> Repo: C:\EnvironmentPortfolio\BS_GodFile. Run only disk-based Python (no `import unreal`): `Content/Python/audit_mi_master_integrity_disk.py` (rerun), `audit_sdf_project.py`, `audit_melusina_asset_integrity.py`, `audit_static_mesh_inventory.py`, plus a hash-diff of the 7 duplicate roots (`Content/_PROJECT`, `Library`, `Characters`, `TurnBasedJRPGTemplate`, `Art`, `Actor_BP`, `Game_BP`) against the primary roots (`Content/Melodia`, `Content/EnvSandbox`, `Content/_ThirdParty`). Deliverables: (1) `Docs/RELEASE_VALIDATION_REPORT.md` consolidating all findings (174 flagged meshes from static_mesh_inventory triaged by severity; the 7 missing foliage texture refs; anything new). (2) `Docs/DUPE_ROOT_QUARANTINE_2026-07.md` — per-file verdict [identical-dupe | unique-only-here | newer-here], flagging the restored `_PROJECT/04_Materials/SDF` masters as unique. DELETE NOTHING. (3) A commit plan (not commits) grouping the untracked repo paths into scoped commit candidates with rationale.
> REVENUE SUPPORT (added ~16:30, Horizon 1 — do after or interleaved, still disk-only): (4) Gumroad SKU #1 ship-checks: dry-run `Content/Python/package_ornament_kitbash.py` prerequisites, verify the FBX SSOT paths from `Docs/MONETIZATION_GEOMETRY_FIX_EXPORT_2026-07-12.md` exist on disk, and reconcile the 6-screenshot checklist against the shot-list drafts in `Docs/Gumroad/Drafts/`. (5) FAB SKU #2 prep: inventory the 61 SDF masters (`Content/_PROJECT/04_Materials/{SDF,baroque,Dev}` + `Content/EnvSandbox/Materials/SDF`) into `Docs/Gumroad/FAB_SDF_PACK_MANIFEST.md` — name, family, prior-audit instruction counts where known, preview-status column for the editor lane to fill.

---

## HANDOFF 4 — Ollama local loops — "Content Data Grind" (text/JSON only)

PASTE (per loop):

> Generate game data drafts into `C:\EnvironmentPortfolio\BS_GodFile\Imports\Data\` as JSON/MD for human review — never write into Content/. Schemas come from existing C++: enemy defs mirror `FMelodiaEnemyDef` (name, HP, speed, element, toughness, BPM, intents[] with name+damage); skill charts mirror `FMelodiaChartNote` (lane 0-3, beat float, pitch int) at 128 BPM in 4/4, 8–16 notes, difficulty ramp. Batches: (1) 12 dungeon enemies across the 4 elements w/ 2-3 intents each; (2) 8 skill charts (2 per element) with escalating syncopation; (3) roguelike room-modifier table (blessing/curse pairs, Nikki-whimsy tone); (4) VN dialogue drafts for Sir Melodious dungeon-entry/exit beats (dark-fairytale-sweet tone, ≤6 lines each).

---

## HANDOFF 5 — Kimi (K-series) — "Level Art & Set-Dressing Lane" (editor owner WHEN SCHEDULED)
*(Kimi's strength: spatial/3D reasoning and level design. It takes editor ownership only when the coordinator hands it the token — never concurrently with the Sonnet executor.)*

PASTE AS FIRST MESSAGE:

> You are the Level Art & Set-Dressing agent for BS_GodFile (C:\EnvironmentPortfolio\BS_GodFile, UE 5.8), reporting to the Fable coordinator. Read `Docs\AI_ORCHESTRATION_HANDOFFS_2026-07-17.md` (rules) and `Docs\VERTICAL_SLICE_20MIN_REVIEW_2026-07-17.md` (targets) first. You drive the Unreal editor through the Monolith MCP (port 9316) ONLY when you hold the editor token — confirm no other agent session is driving it before you start (`tasklist | findstr UnrealEditor` + ask the user).
> HARD RULES: finish existing systems, never invent parallel ones; NEVER delete or overwrite existing assets — create new, commit with explicit path lists; C++ is NOT your lane (report needed code changes to the coordinator instead); reflection C++ = cold build only (context: Live Coding crashed the editor twice on this project); branch state: `feature/touchdesigner-mcp-integration` is local-only (LFS budget blocks push) — keep commits local, never force-push, never rebase main.
> PROVEN RECIPES (use, don't rediscover): GeometryScript procedural meshes (`/Game/EnvSandbox/Meshes/Celestial` recipe: append primitives into DynamicMesh → solidify grid 96 → dual perlin → plane-cut → recompute normals → create_new_static_mesh_asset_from_mesh); ISM instancing via SubobjectDataSubsystem with `Transform(rotation=[PITCH,YAW,ROLL])` (FRotator order!); ornament kit `/Game/EnvSandbox/Meshes/Ornament` (flat panels need pitch=90); SDF material instances `/Game/EnvSandbox/Materials/SDF/Instances` + restored math-art masters `/Game/_PROJECT/04_Materials/SDF`; verify materials via `get_instance_parameters` NOT `render_preview`; SceneCapture2D screenshots (never viewport-screenshot user levels); PointLight non-inverse-squared intensity 5–10.
> MISSION (slice-gating art, in order):
> 1. **Bedroom** (`/Game/Melodia/Levels/Opening/L_MelusinaMorning`): author a real bed (GeometryScript or kitbash from melusinashouse assets) to replace `MelodiaBedActor`'s placeholder cube; dress the room to dark-fairytale-sweet (lantern warmth, filigree trims, sheet-music motifs).
> 2. **Dungeon room dressing pass**: the 22 room templates under `/Game/Melodia/Roguelike/Rooms` are greybox-ish; dress the V1 of each archetype first (8 archetypes) using the ornament kit + SDF materials + sakura Niagara suite — silhouette first, 15-30 min per room, SceneCapture verification shot per room committed to Docs/RoomDressing/.
> 3. **ZenForest** (`/Game/ZenForestTest`): beautification pass along the golden path only (player route to the dungeon gate) — do not restructure terrain or lighting rig.
> 4. After each numbered item: save, scoped commit, report with screenshots and a one-paragraph summary to the coordinator.
> NIKKI/REVENUE ADDITION (~16:30): every space you dress gets ONE deliberate "photo spot" — a composed SceneCapture angle (saved camera transform + framing note in `Docs/RoomDressing/`) worthy of a Steam trailer frame or store screenshot. These shots are Horizon-1/2 revenue assets (Gumroad/FAB listings + wishlist trailer), not just polish — compose like an Infinity Nikki photo-mode moment: strong silhouette, one light story, one sparkle accent. Sequencing: finish the website-render task first; take the editor token only after the Sonnet lane's UDS repair + PIE verifications are done.

## HANDOFF 6 — GPT — "C++ Finisher Lane" (2026-07-18; replaces DeepSeek's C++ role)

PASTE AS FIRST MESSAGE:

> You are the C++ Finisher for BS_GodFile (C:\EnvironmentPortfolio\BS_GodFile, UE 5.8; gameplay C++ in Plugins\MelodiaCore\Source\MelodiaCore\). Read `Docs\AI_ORCHESTRATION_HANDOFFS_2026-07-17.md` (rules + State Update 2) first. HARD RULES: finish existing systems only — every change extends an existing class at an existing seam, cite the doc/handoff item for each task; battle/turn math is FROZEN; builds only with no UnrealEditor.exe running (`Build.bat BS_GodFileEditor Win64 Development -Project=<uproject> -NoUBA -MaxParallelActions=4`) — Live Coding is banned for anything touching UCLASS/USTRUCT/UENUM/UPROPERTY/UFUNCTION; scoped commits with exact paths; never rebase/force-push; coordinate the build window with the user (the Sonnet editor lane may be running).
> TASK LEDGER, in order:
> 1. **Audit DeepSeek's monetization arrows**: `MelodiaSaveGame.h` gained Currency/HeartMelodyTokens/SwirlMelodyTokens/OwnedContentPacks fields but no commit is visible on main. Locate the state (main working tree vs `feature/touchdesigner-mcp-integration`), verify M1 capture/restore wiring actually exists in `MelodiaSaveGameSubsystem.cpp` (capture in SaveToSlot, restore in HandleAsyncLoadComplete, via owner-class Restore* methods mirroring `RestorePersistentPartyState`), verify M2 (file-backed `IMelodiaEntitlementProvider` reading `Saved/DevEntitlements.json` + `RefreshCatalog` call at startup). Finish whatever is missing, cold-build, commit clean.
> 2. **Calibration hookup**: `MelodiaAudioComponent`'s BeginCalibration/RecordCalibrationTap/FinishCalibration flow ships but nothing calls it. Wire a minimal entry: console command or debug key binding through the existing `UMelodiaBattleInputComponent`, persisting `InputLatencyOffsetMs` via the save subsystem. No UI widget — the settings screen consumes it later.
> 3. **OSC battle events → TouchDesigner** (Kimi's documented live-gap): in the next cold-build window, emit beat/grade/break/victory from the existing `UMelodiaRhythmReactivitySubsystem` notify points to the Envoy/OSC bridge already configured (port 9870, see TouchDesigner docs in Docs\). Reuse the subsystem's signal struct; do not add a second event system.
> 4. **Standby**: pick up any C++ fixes surfaced by the Sonnet lane's PIE verification of the four dungeon arrows (defeat path, persistence, archetype, generator contract) — their expected behaviors are documented in the handoffs doc.
> DELIVERABLE per task: diff, `Result: Succeeded` build log, 5-line PIE-tester summary.

## Coordinator cadence (Fable = this chat)
- User + Fable review each lane's deliverables; Fable merges (imports Cursor's files via the Sonnet chat's queue, folds validated data into `MelodiaSongSkillLibrary`/enemy definitions, applies the quarantine decisions the user approves).
- Editor-access arbitration: Sonnet chat holds the editor; Fable takes it back only for judgment-heavy passes (feel tuning, cut-in choreography, slice assembly).
- Slice assembly order: battle feel verified → dungeon loop content (ollama data in) → UI WBPs (Cursor specs → Sonnet authoring) → bedroom-morning bookends w/ save → 20-min playtest → polish list.
> **Superseded execution record (2026-07-26).** Retained as historical evidence.
> Do not execute its agent prompts or MelodiaCore/roguelike backlog as current
> authority. Active work is controlled by `QUEUE.md` and
> `MELODIA_UE58_INTEGRATION_ARCHITECTURE_2026-07-26.md`.
