# Sea Above — Finalized Execution Plan (2026-09-03)

**Goal: the loop reads as a composed dream-sea — golden-ratio pacing, Nikki-grade
presentation, horizon that lies beautifully — with every asset AAA-verified.**

## Status locked so far (reload-verified tonight)

- 136 height-aware placements persisted (ReefGarden 84, AbyssFloor 40,
  CathedralHalo 12) — manifest `specs/water_veil/sea_above_heatmap_dress.v1.json`
- Ribbon beat live: `PCG_Ribbon_XylophoneTrail` (XylophoneTrail, 83 inst) — reads the
  3 PathFalloff spline actors
- Garden beat live: `PCG_Ribbon_GardenBeat_BellTree` (BellTreeGarden, 27 inst) at
  music-key centroid
- Materials: all 136 placed pieces materialed (CoralSkin/Sand/Jelly_Bell);
  palace kitbash on Copernicus MIs; zero greybox leak
- Cathedral tier verified: 170/178 `_PROJECT/04_Materials` live; SDF cathedral
  family + Cosmo master + ocean masters confirmed AAA

## Execution order — each step one editor transaction, reload-verified

### A. Post-processing stack (Nikki grade) — next
- ONE unbounded PostProcessVolume in LV_SeaAbove_Prototype
- Blendables (priority order): `MI_StorybookOutline_GameplayStandard` (outline),
  `MI_MeluColorGrade_GameplayStandard` (teal grade), optional StarryNight overlay
- Verify: reload-proof; owner picks Narrative vs Hero profile by eye

### B. Living horizon (schools + silhouette island ring)
- Place `NS_SchoolOfFish_WispFin` + `WispFinVWide` (Oceanology plugin) in the
  prototype water near palace + mid-distance — zero new assets
- Place `NS_SDF_ParallaxFish` (Sakura system) — parallax fish card, "detail that
  isn't"
- Silhouette island ring at GOLDEN radii along spiral (5k/8k/13k/21k/34k/55k):
  flat fog-tinted silhouette cards (rock/cathedral-spire shapes), cost ~2-4 tris
  each, they ARE the LOD — manifest from heatmap pipeline, AAA fog-grade MIs only
- Far-field jelly billboard swarm near horizon matching Melusina-cathedral
  silhouette (no new meshes)

### C. Golden density upgrade (in-flight, deferred)
- Reef scatter path-falloff -> phi decay `(D/(d+D))^phi` (generator one-liner,
  re-apply when A+B verified)

### D. Gated: Houdini VDM atmosphere bake (decision gate)
- VDM bake for FAR-FIELD atmospheric masses only: jelly-fog bank, plankton
  lamina, cathedral spires dissolving at 40k+. Niagara schools already beat VDM
  for living shoals — do NOT VDM the animals. BK if A+B+C read well first.

## Authority & safety (binding)

- One editor (:9316), one writer. All editor mutation through this session's
  harnesses; never two MCP surfaces.
- No new landscape, no new material masters. AAA tier only:
  Toon_Universal(_Alpha) for reef/jelly, Oceanology/Grand for water,
  _PROJECT/04_Materials SDF family for cathedral, lookdev post profiles for grade.
- PCG: extend LIVE graphs only (Xylophone/XylophoneTrail/BellTree/Cathedral
  family). Hollow scaffolds stay untouched (review doc).
- Evidence: every step = manifest + apply harness + reload count, gate row or
  evidence envelope in Saved/.
- NEVER: git clean/checkout, delete_asset on uncreated assets, skill BP reads,
  FBX into occupied paths.

## Delegation plan (this session)

Parallel subagents (read/review/prep ONLY — no editor mutation):
1. PPV profile investigator — map lookdev profiles -> recommended stack for a
   daylit dream-sea, output PPV spec JSON (blendables, priority, radius)
2. Swarm placement prep — Oceanology school + ParallaxFish placement spec
   (positions, scales, count tuning, Niagara settings to verify in editor UI)
3. Island-ring geometry prep — silhouette card mesh/asset hunt + manifest
   generator extension for golden-radius ring
4. Contract reviewer — check this plan + the 3 review docs against AGENTS.md /
   convergence contracts; flag authority violations, dead refs, hollow graphs
5. VDM feasibility — Houdini side: jelly-fog VDM bake recipe, input field
   contract, cost estimate; no UA writes

Each returns a spec/JSON + evidence, NOT editor mutations.