"""Melodia Rhythm Battle — CLI entry point.

Allows:
    python -m gmm              ← interactive game hub
    python -m gmm --battle EnemyId  ← quick single encounter
    python -m gmm --help       ← this message
"""
from gmm.main import main

main()
